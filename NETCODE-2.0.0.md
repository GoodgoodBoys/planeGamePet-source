# PC 对战同步改造与验收

## 范围和版本

- PC—PC 客户端/服务端新增 2.0.0 移动协议；不修改 `shared/plane_sim.h` 或 `shared/plane_protocol.h`，不改变 PC—ESP32 场景。
- 新增能力位 bit 7；输入/原子状态/同步中止使用 PC 专用消息 14/15/16，仍在原有 192 字节包上限内，现有鉴权和 WSS 隧道不变。
- 1.x 与 2.x 不能对战，已有绑定保留；旧客户端之间继续使用原有模拟。2.x 之间小版本兼容。旧服务器缺少能力时显示“服务器需更新”，不静默使用旧移动规则。
- 此次只制作候选程序和更新 PlanePet 自身服务；不发布 GitHub/OSS 更新清单、不修改 FingerKnight、Nginx 或防火墙。

## 规则

1. 60 Hz 固定步长；鼠标目标与键盘共用 90 世界像素/秒向量速度上限。键盘斜向归一化；鼠标目标在本方边界内裁剪，接近时精确停止。任意移动键按下时优先使用键盘，松开后恢复仍按住的鼠标拖动。
2. Q8 定点位置，服务器与客户端共用整数移动函数。单步最多 1.5 像素；像素显示四舍五入有不足 1 像素的栅格误差。不改变 240×320 地图、52 像素独立 HUD、240×372 游戏客户区和桌宠尺寸。
3. 输入序号独立于网络包号，并带本局 ID；每包最多重发 48 个输入，待确认最多 60 个。服务器初始缓冲 4 步，每个模拟步最多处理一个输入；缺号停住，不跳号、不重复执行、不因输入洪泛加速。
4. 客户端保存未确认输入；以服务器准确到小数的确认位置为基点重放。不存在旧代码的逐帧 ±2 修正或误差 >24 瞬移。运动本身无击退/复活力，重放应该精确一致；否则中止，不掩饰错误。
5. 客户端卡住超过 250 ms、待确认队列超上限或重放不一致时申请中止；服务器连续 2 秒没有输入推进也中止。双方显示“本局中止 / 网络或运行卡顿，同步中断”。中止不计为胜负平，不更改旧历史文件格式。
6. 玩家飞机本地预测，以固定步的前后位置插值显示；对方飞机和子弹在同一个延迟 67 ms 的服务器状态时间线上插值。子弹 ID 不跨弹混用，不在客户端自行扣血。
7. 子弹仍每 0.8 秒自动发射，速度 180 世界像素/秒。用子弹与飞机的相对连续运动检测碰撞；命中仅扣血，不回出生点。同一步致命命中先累计后扣血，避免数组顺序决定胜负。

## 网络边界（不能承诺任意网络下零延迟）

- 已确认重放消除的是错误校正造成的瞬移，不会消除显示器像素格、低帧率或真实网络延迟。
- 命中与血量由服务器统一决定。自己的飞机是即时预测，远端画面有插值缓冲；高延迟下可能稍后才收到命中通知。此版本不实现攻击者视角历史倒带，也不承诺两个不同时刻的本地画面与权威碰撞逐像素重合。
- 输入流中断不会伪造移动；严重卡顿明确中止，不尝试断线重连继续旧局。
- 新同步中止仍使用兼容的 `game_finished`、`game_end_reason=ServerUnavailable` 统计，不新增敏感字段。此类中止不上传正常胜负 `game_outcome`，已有数据库/统计字段、同意机制、旧记录不迁移也不清空。

## 验收入口

- `tests/pc_motion_test.cpp`：速度/目标停止/边界/镜像、相对连续碰撞、射速、无复活、重复包/截断包/窗口限制、确认重放。15 个 120 秒模拟场景：RTT 0/50/100/150/200 ms × 30/60/120 FPS，附加每方向 0–20 ms 抖动与 2% 丢包。
- `tests/motion_server_cases.h` / `tests/motion_client_cases.h`：直接编译真实服务端/客户端状态逻辑，覆盖兼容性、限速消费、中止、换局清理和历史兼容。
- `tests/motion_live_test.py`：两个真实 Windows 测试客户端，经独立 loopback 损伤代理连接真实服务器；输入走生产鼠标坐标映射，检查位移与命令步数、双方最终血量/结果，导出真实游戏渲染 BMP。
- `verify_fix_regressions.ps1`：旧联网流程、配对持久化、邀请、勿扰、统计、升级失败回滚和单/双端包完整性。
- `verify_release_hardening.ps1`：现有全量 Windows 布局/文字/窗口/交互回归。

## 2026-09-07 交付验收结果

- Windows 完整编译通过；核心/客户端/服务端额外 `-Wall -Wextra -Werror` 静态编译通过。Linux 使用相同源码编译并通过相同移动、碰撞和服务状态测试。
- 最终移动/协议/画面时间线测试 **777,476 条断言通过**。15 个虚拟网络场景累计 30 分钟模拟；确认重放造成的位置修正均为 0。
- 真实 Windows 双端，经 200 ms 注入 RTT、每方向 0–20 ms 抖动、2% 丢包，完成对战，双方血量与结果一致。缓存优化后报告：`dist/motion-live-33afb1d879ea4db88da3cf38379ffc99/result.json`。
- 最终公网正常对战通过：`dist/motion-live-5c37037b26174cdc80ab8ccd9af7cb01/result.json`。双方分别记录 356/358 个活动帧，约 39.8/39.9 Hz，最大未确认队列 20 步；所有相邻位置变化均未超过已执行输入步数对应的速度上限，未触发位置纠错或同步中止，双方最终 HP=0/0，均判平局。此前公网胜负局也通过，双方 HP=1/0，胜负战绩各记录一次。
- 最终公网异常中止也通过：测试专用驱动模拟一次本机长卡顿，两端均收到中止标记并显示明确原因，HP=3/3，总战绩、胜、负、平的计数均为 0，不把中止计为平局。报告：`dist/motion-live-038dff9d7631431e8535d49195915c53/result.json`。测试驱动只在专用编译宏中存在，不进入交付程序。
- 现有 **702/702 Windows 窗口与交互检查通过**：`dist/release-regression-a302a95a4f21455f817c34c7ba190af9/中文 用户路径/检查结果.txt`。
- 29 项网关/统计测试、真实客户端/服务端状态回归、旧协议完整对局、配对/恢复/解绑/版本规则、升级替换与失败恢复、两个 ZIP 静态检查均通过：`dist/motion-regression-delivery.log`。
- 帮助正文 18 节同步检查通过；嵌入客户端、隧道、升级器与对应最终独立 EXE 的 SHA256 完全一致。飞机、拳头与爱心原始素材未修改。

### 画面与性能

实机定位到每帧六次缩小 2172×724 爱心图集的绘制开销，仅为 13×12 HUD 爱心增加三份缓存。此次测试机双窗口的绘制平均耗时由约 70.7 ms 降至 6.3 ms，活动更新频率约由 13 Hz 升至 40 Hz。这是本机测量，不代表所有电脑固定帧率。

已读图核查两端运动帧、最终结算帧、两侧 HP 0–3 的全部组合。地图/HUD 分离、飞机边界、文字和按钮未改变尺寸或位置。缓存前后心形轮廓相同，预乘透明合成使部分半透明边缘 RGB 最大相差 3/255；不存在缩放或色彩方案调整。51 张对照图字节完全一致；动画时刻/勿扰倒计时相关图不以文件哈希代表布局变化。

### 服务器与数据保护

- 最终服务端 SHA256：`726d6828dd166b84e96e03e10ca4255a9c977c7ed2a6ece98be28a761adc2a2e`。
- 最终备份：`/opt/plane-pet/backups/motion-2.0.0-final-rrwupcdc`。变更只覆盖 PlanePet 服务端程序；原有依赖导致 PlanePet 网关一同重启，网关代码/配置未改变。
- 首次保护检查因本项目网关 PID 随依赖重启而自动回滚；只读确认 FingerKnight 等均未变化后，才按现有依赖完成切换。未修改依赖配置来规避检查。
- 最终核对 FingerKnight PID 81630、旧设备服务 PID 15737、Nginx PID 30712 均保持原值；受保护文件哈希不变。
- 公网测试不上传遥测、不新增网关凭据。仅解除本次创建的随机测试绑定、删除两份测试凭据副本；不删除用户数据。测试后 `server_bindings.db` 与 `gateway_tokens.db` 和部署前备份哈希完全一致。
- 未上传 GitHub、OSS 或自动更新清单；此轮交付可直接运行的候选包。

### 最终交付包

| 文件 | SHA256 |
| --- | --- |
| `dist/PlanePet-Public-Single-2.0.0.zip` | `76023490b43c3ae2c680a5405236ec51289f139d27abdb0f4bb4bf7a68d41af1` |
| `dist/PlanePet-Public-DualLocal-2.0.0.zip` | `32fe9dec470476cede22509469ea9ccb5065b008a87fc40c901a657a14c75774` |

先退出旧版，再运行新包中的 `PlanePet.exe`。两个包使用同一套最终客户端。现有发行物仍未进行 Windows 商业代码签名，不能将通过功能验收等同于已消除 SmartScreen 提示。通过已执行项目不等同于对所有设备和网络的“绝对无 bug”保证。
