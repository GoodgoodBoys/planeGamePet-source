# 总连接限制与在线规模邮件提醒验收

日期：2026-09-09。范围仅 PlanePet 网关与新增提醒模块，不修改客户端 EXE、游戏同步逻辑、FingerKnight 或 Nginx。

## 已上线

- `PLANE_PET_MAX_CONNECTIONS=0`：不设业务总连接硬上限，管理员状态接口实际返回 `connection_limit: 0`。
- 保留每安装实例 2 连接、Nginx 每 IP 64 连接/握手速率、每 IP 每小时 64 新登记、1000 累计凭据保护；当前累计凭据 16，余量 984。取消并发上限不等于取消这些限制，也不构成承载能力承诺。
- 100 / 200 / 500 个成功认证握手连接的首次阈值记录已启用，独立数据库 `/var/lib/plane-pet/online-alerts.db`。当前未达到阈值，记录为空，无伪造测试人数。
- SMTP 模块和独立服务定义已安装，但**邮件发送服务尚未启动，也未启用开机发信**。尚未安全配置重新签发的邮箱授权码。

## 验收结果

1. Windows 本地网关回归 42 项通过；包括不限总数时第 501 条连接接入、仍支持显式正数紧急上限、单安装/IP 防滥用和统计兼容。
2. Linux 独立暂存目录中：42 项网关、21 项提醒、3 项备份，共 **66 项全部通过**。
3. 提醒测试覆盖阈值边界、短暂峰值、跨过多个阈值、人数回落、重启去重、数据库损坏不重建、后台写盘不阻塞事件循环、持久化失败重试、进程互斥、SMTP 指数退避与模糊结果隔离。
4. Windows 本地 500 常驻模拟连接 + 第 501 条接入通过，共 11,501 次 WebSocket/UDP 往返，3 轮共 48 次重连；中位数 45.94 ms、P95 58.96 ms，最终计数归零、凭据未改变。仅本机明文转发测试，不含 TLS/Nginx/游戏模拟，不是公网性能结论。
5. 正数上限兼容测试：128 常驻连接、第 129 条按配置拒绝；这是离线测试，不是当前线上上限。
6. Linux 仅做 16 + 1 条隔离连接小测试，未对共享 ECS 做 500 人压测。
7. Python 编译与 `git diff --check` 通过；systemd 服务定义验证通过。验证中提示已有 `snapd.service` 的 `RestartMode` 警告，与本项目无关，未修改该系统服务。
8. 上线后 `/healthz`、`/readyz`、认证状态接口、提醒 SQLite 完整性均通过；持久化待处理数、错误、统计丢弃和写入失败均为 0。
9. 从本机访问公网 `https://8.166.124.212:32112/healthz`，证书正常验证、返回成功。服务器到 `smtp.163.com:465` TLS 及 EHLO 成功；**未进行 AUTH、未发送邮件**。

## 部署保护

部署在零在线连接时执行，仅重启 `plane-pet-gateway.service`。部署前后的 333 个受保护文件联合 SHA256 一致：

`019554c79bdeeb231ebc6cfd32ce441a741e4da4d36e3c3a3ecd2269ede62168`

PlanePet 游戏服务、FingerKnight lobby、FingerKnight server（原本未运行）、plane-link、Nginx 的 PID 和运行状态全部不变。原绑定、凭据与保留期内的统计行逐项核验通过。

源码暂存：`/opt/plane-pet/staging/online-alerts-20260909-vezB2Gyz`。

切换前备份和部署记录：`/opt/plane-pet/backups/plane-pet-20260909T083911Z-c0dbda36`。只允许针对 PlanePet 做回滚，不回滚、覆盖或启动其他项目。

## 尚需完成

邮箱授权码曾出现在聊天中，不使用该码。运营方应撤销它，重新签发后仅保存在本机私有文件 `C:\Users\81228\Desktop\alive\Key\plane-pet-smtp-auth.txt`。

收到已安全保存的确认后，再通过 SSH 加密传输配置 root/0400 文件和 systemd 凭据，发送一封明确标注“配置测试”的邮件；测试不访问/消耗终身阈值记录。验证 SMTP 接受与运营方收件后，启用独立发送服务并复核去重状态。现阶段不可宣称邮件提醒已经可用。

SMTP 不承诺绝对 exactly-once 投递；结果不确定时停自动重发，人工核查。数据库需保留并备份，不可随匿名统计清零或旧版恢复回滚。维护细节见 `deploy/ONLINE-ALERTS.md`。
