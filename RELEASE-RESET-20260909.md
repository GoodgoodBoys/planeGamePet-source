# 1.0.0 基线 → 1.0.1 更新验证

本次按开发者明确要求，对当前完整功能版本重新编号。1.0.0 是安装起点；1.0.1 是同一套功能的升级验证版，仅提升修订号和版本说明。两版均属于 `release_epoch=1`、`release` 通道，使用相同的游戏协议、更新验签公钥和数据目录，最低支持版本为 1.0.0，属于可选更新。

保留当前飞机/云/表情/受击素材、帮助与关于、勿扰、绑定与战绩、同步机制和已确认修复。不会为测试升级修改真实绑定、设置、战绩或统计。

构建和本地验收已完成，GitHub 发布正在进行。OSS 网页自动化通道持续超时；Firefox 备用通道因不支持网址策略校验而停止。本轮尚未上传 OSS、尚未删除旧云端版本，不能声称公网自动升级已经可用。

## 冻结制品与验收

| 版本 | 单端 EXE SHA-256 | 完整 ZIP SHA-256 |
| --- | --- | --- |
| 1.0.0 | `37d9c6bb4fb718fb2110906cd8a2336f4c72dfe321a7676a1bae7f50ce5dfa7a` | `2af4dd2618c814c6d695cddcee0410645b46677161d0e468d1ade94db16bd81a` |
| 1.0.1 | `e2978d39652b67cbd394630d23be3c4076702fc6cde30c010db7384911e54b28` | `cf18c1910abeb0d29888ef543ac4b17f30ae0b2f4650b9fd3c50a70ae1c56571` |

冻结文件位于 `release_out/reset-20260909/plane-pet/windows/release/`，latest.json 已签名指向 1.0.1，minimum_supported_version 为 1.0.0。该目录中的文件仍是本地发布材料，不代表云端已存在。不要重编译后覆盖这些已冻结文件。

- 1.0.0 源码基线：`4ac01ab136c867d2817584b69aaeee8d19a19754`，新标签 `public-v1.0.0`。旧 `release-v1.0.0` 不改写。
- 完整构建通过；1.0.0 全核心回归通过，含 42 项网关/统计、406 项本地存储与更新安装/回滚/中断恢复。
- 1.0.0 更新网络和原生更新窗口测试在正常 Windows 环境通过；受限沙箱不支持 TLS，未降低安全校验以跳过此项。
- 1.0.1 完整原生窗口回归 **787 PASS、0 FAIL**。初次沙箱运行的任务栏/真实鼠标四项失败已在正常桌面环境复验通过，未改业务逻辑掩盖环境限制。
- 两版单端/双端发行包、11 项发行前置拒绝检查、内嵌运行组件与素材摘要校验通过。
- 保留的真实 1.0.0 更新代码已离线验证 1.0.1 RSA、SHA-256、长度、最低版本和正式通道；篡改签名、旧通道及 epoch 不符均拒绝。
- GitHub 安装包从冻结 ZIP 解压并逐个比对 EXE 摘要，未误用后续同名重编译产物。

仍需在 OSS 可操作后完成：清理精确盘点的旧 PlanePet 公开发行对象，上传两个冻结版本及清单/签名；用 1.0.0 程序执行真实公网检查、下载、替换重启、档案保留与 1.0.1 最新版本检测。清理前先保存旧清单/发行对象，不清理用户数据或 ECS 救援备份。

## 发布与清理范围

仅处理 PlanePet 的公开发行文件和更新清单；最终保留新基线 1.0.0 和更新目标 1.0.1。Bucket 内其他项目、ESP32 固件、权限配置不变，ECS 运行服务、用户数据和救援备份不清理。

旧 Git 提交和标签保留，不强制覆盖历史。旧测试更新通道清理后，历史测试版需手动下载本次 1.0.0；旧数字更大的测试版本不会自动降级。

## 用户测试

1. 从旧程序菜单选择“退出”，不要只隐藏桌宠。
2. 下载本次 1.0.0，运行 `PlanePet.exe`，不要删除 `%LOCALAPPDATA%\PlanePet`。
3. 在右键菜单“帮助与更新 → 检查软件更新…”确认检测到 1.0.1，选择升级。
4. 等待下载、验签和重新启动；关于页应显示 1.0.1，绑定、设置和战绩应保留。
5. 再检查更新，应提示已是最新版本。

此前拒绝更新的七天静默可能抑制自动弹出，但手动检查不受影响。两个制品暂未有 Windows 发布者签名，更新清单仍使用原有 RSA 签名及 SHA-256 检验，不关闭任何更新安全校验。
