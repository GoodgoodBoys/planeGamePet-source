# PlanePet 3.0.7 正式上线前复核

检查日期：2026-09-08，北京时间。范围：Windows PC→PC 客户端、启动器/隧道/升级器、配套服务器与匿名统计网关、发行包及发布/运维准备。保留旧 PC/设备协议的回归，不扩展修改 ESP32；不操作 FingerKnight。

本轮是审查，不是修复或发布。除审查报告与本地隔离测试产物外，未修改业务代码、用户档案、线上数据或配置。未签发证书、重启服务、执行线上对战/压测、发布 OSS 或推送 GitHub。

## 结论

现有核心功能可以继续受控测试，但不能把当前候选包视为已经完成正式公开发行验收。已确认的缺口主要是发布流程、专用证书任务范围、隐私文案及运营保障；另有一项过期测试夹具导致更新专项失败。没有把这些问题混称为“游戏功能全部有 bug”，也不以自动测试通过承诺所有环境无缺陷。

## 必须完成的清单

### 1. 限定证书续期范围，并验证真正续期与加载

**证据：**线上 `/etc/systemd/system/plane-pet-cert-renew.service` 执行 `/opt/certbot-ip-v5/bin/certbot renew ...`，没有 `--cert-name`，会扫描同机证书；deploy hook 为共享 Nginx reload。9 月 7–8 日日志多次出现 `Could not select or initialize the requested installer nginx`。任务退出码为 0，不能单凭该状态证明证书已续期。

当前 IP 证书有效期至 **2026-09-12 21:22:08（北京时间）**，HTTPS 健康检查正常。该证书自身使用 webroot，并非 nginx installer；因此尚不能据此认定 IP 证书已续期失败，日志也可能来自同机其他证书。没有读取其他项目证书的私钥或修改其配置。

**完成标准：**只管理 PlanePet 明确拥有的证书；经授权做限定证书的续期演练，核验验证路径、签发、部署 hook 和公网实际提供的证书；配置到期和续期失败告警。共享 Nginx 操作需保护其他项目，不可直接运行现有全局 renew 来“试试看”。[Certbot 续期说明](https://eff-certbot.readthedocs.io/en/stable/using.html)

### 2. 发布统一的线上版本，并验证跨版本升级

**证据：**本地版本为 3.0.7；同日实际读取 OSS stable 清单仍为 1.0.2，其 EXE 返回 200。当前线上服务器摘要与 3.0.1 部署记录一致；3.0.7 为客户端视觉修订，不能因客户端版本较新就推断服务器必须重装。

**完成标准：**确定正式版本后发布不可变单端文件、完整 ZIP、更新说明和签名清单；保留旧文件。单机双端包不进入普通用户升级通道。用真实 1.0.2 及已有 3.x 候选版本分别验证“检测→下载→安装→恢复绑定/设置/战绩”，再验收好友大版本不一致的阻断与恢复。既有 1.0.2→1.0.3 升级演练不等于已验证 1.0.2→3.0.7。

### 3. 补齐发布脚本的防误发检查，并修正过期验收

**已确认：**`release/New-PlanePetUpdateManifest.ps1:45` 对版本目录使用强制复制；生成清单前未核验参数版本与 EXE 内置版本一致、最低支持版本关系、成品 Authenticode 状态，也没有阻止同版本不同文件覆盖。运行时验签只能确认清单来自签名者，无法替代发布者自身的内容校验。

**另一个已复现问题：**`tests/update_state_test.cpp:126` 把固定 `2.0.0` 当新版，随后要求 `canDownload=true`。当前 3.0.7 正确拒绝降级，因此 `manual_joins_automatic_check` 失败。不是已证明的客户端升级功能故障，但 `verify_update_windows.ps1` 因此提前终止，不能记录为全绿。

**完成标准：**发行入口强制核对内置版本、合法版本范围、最低版本、签名和摘要，拒绝同版本不同内容；测试使用相对当前版本的未来版本并保留显式拒绝降级用例；重新执行完整更新专项，不跳过失败项目。

### 4. 完成 Windows 发布者签名及干净环境运行检查

**证据：**当前单端 EXE 的 Authenticode 状态为 NotSigned。构建已有 `-RequireSignature`，但当前包是未签名候选包。

**完成标准：**确定发布者和合法签名方案，先签内嵌组件，再签启动器，最后生成 ZIP/摘要/更新清单；在干净 Windows 和实际安全软件策略下验证。更新清单签名与 Windows 发布者签名是两回事。签名降低分发阻力，但不保证新文件立刻免除 SmartScreen 提示。[微软说明](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation)

### 5. 修正隐私说明，并建立可执行的反馈/数据删除流程

**已确认文案错误：**`build.ps1:260`、`:311` 生成的随包说明声称“原始安装凭据不会上传”。实际 `tunnel/main.cpp:381` 将令牌放入 HTTPS Authorization 请求，网关收到后再计算摘要（`gateway/plane_pet_gateway.py:132`）。正确含义应为“通过 TLS 传输用于鉴权，服务器持久化摘要而非原始令牌”，不是“不传输”。这不是发现了明文网络传输。

`PRIVACY.md` 仍标注适用 1.0.4，反馈仅指向测试发起者；没有正式运营者名称、联系方式及服务器数据删除申请处理流程。本地清除并不删除服务端资料。持久安装标识能关联同一安装，不应把它描述为绝对无法关联或当然免于个人信息保护要求。

**完成标准：**同步首次提示、随包文档、内嵌文档及帮助；明确可选统计与必要联网数据、保留期及运营日志边界；提供可验证申请归属的删除渠道，明确解绑、远端数据处理及备份到期顺序。无需为此强行增加复杂账号系统，但流程应真实可执行。[个人信息保护法](https://www.samr.gov.cn/wljys/gzzd/art/2023/art_3ef1e889c1e644d4b65b5f5c7f432386.html)

### 6. 落实自动监控、加密异机备份与故障处理

**证据：**已有备份/恢复工具和手工演练。线上专用定时器检查只发现证书续期；常见 cron 目录未发现 PlanePet 专用任务。现有 `/opt/plane-pet/backups` 为发布/修复时创建的同机备份，最近为 9 月 8 日部署备份。不能将未发现解释为证明不存在任何外部运维平台，但当前项目没有可验收的异机备份/告警证据。

**完成标准：**确认告警接收人、专用私有备份位置、保留周期；落地服务就绪、证书、容量、磁盘、统计丢弃/写入错误和备份年龄的监控。定期验证恢复。备份包含凭据，不能与公开下载文件混放。故障处理与回退只覆盖 PlanePet；不恢复共享服务器整盘来回退本游戏。

### 7. 确定公开规模、容量与防滥用策略

**线上现值：**128 条并发隧道、1000 份累计登记、每 IP 每小时 64 新登记；Nginx 同 IP 连接上限 64。审查时 0 在线、16 已登记、984 剩余，统计队列/丢弃/写入失败均为 0。128 是在线客户端连接数量，不是 128 场对局；重装也可能新增凭据。

**完成标准：**先给出首批人数与预计同时在线数，做隔离的完整 WSS→网关→游戏→统计链路容量测试；明确达到容量后的扩容/拒绝策略，以及公开注册被刷、同一办公网络集中接入时如何处理。现有 128 条本地网关压测不是共享 ECS 的完整承载证明。不能删除永久绑定腾名额，也不能在 FingerKnight 所在生产机无约束压测。

### 8. 完成独立双机、长期运行和最终联网验收

**证据：**已存在同机双端公网与弱网验收；当前 3.0.7 的自动渲染、逻辑和窗口回归通过。没有本版本两台独立电脑、混合 DPI/拔屏、真实跨卷升级、磁盘满、安全软件、至少两小时持续运行的完整验收证据。

**完成标准：**两台干净 Windows 10/11、不同网络完成全流程；鼠标拖动/键盘切换、边界命中、断网中止和战绩不重复；测试休眠唤醒、网络切换、隐藏后邀请、实际混合 DPI；长时间记录 CPU/内存/GDI/USER 句柄趋势。故障注入只使用测试副本。32112 非标准端口在受限办公网仍需实际验证，不默认绕过组织网络政策。

### 9. 归档可复现源码、素材与发布文档

**证据：**本地 Git HEAD 为 `e15b7c0`（1.0.2），本轮报告新增前有 140 项已修改或未跟踪条目，其中包括 3.x 核心及素材。不能将当前工作目录等同于已经保存好的 3.0.7 发布标签。`RELEASE-ACCEPTANCE.md` 仍是 1.0.3 表单，隐私版本号也落后。

**完成标准：**归档全部实际使用的源码/素材/导入脚本，排除私钥、用户档案和后台数据；建立发布标签和成品哈希对应关系，并从干净目录构建一次。同步当前版本验收表和更新日志。确认素材来源与再分发权；若公开源码，明确许可意图，不将“公开仓库”误认为已授权任意使用。已有编译器运行时许可文本随包，不能混称为完全缺少许可文件。

### 10. 确认中国境内公开发行的适用要求

本程序包含真实联网对战，不能仅因名为桌宠、免费或通过 OSS 下载，就自行认定不涉及网络游戏公开发行要求。国家新闻出版署的审批指引覆盖客户端等游戏类型，并列明出版、运营与未成年人保护相关要求。

**完成标准：**先明确受控研究测试还是面向公众运营，向所在地主管部门或专业法律人士确认本项目适用要求及所需资质；如采用自有国内下载站/CDN，再单独确认域名备案。此处是风险识别，不是已判定违法或要求立刻加入收集身份证的功能。[国家新闻出版署指引](https://www.nppa.gov.cn/bsfw/xksx/cbfxl/wlcbfwspsx/202210/t20221013_600725.html)

## 本轮实际验证

| 项目 | 结果/边界 |
| --- | --- |
| `verify_fix_regressions.ps1` | 通过；客户端/服务端边界、绑定取消/复用/恢复/解绑、大小版本、勿扰、表情、旧场景集成、更新安装/回滚/哈希拒绝/中断恢复、两包静态验证 |
| 网关与统计 | 上述入口运行 35 项测试通过；包括畸形请求、注册限额、数据库忙不阻塞、去重、旧数据、异常结束 |
| `verify_release_hardening.ps1` | 本轮正常桌面环境 702 PASS / 0 FAIL |
| `verify_battle.ps1 -SkipLive` | 777476 项移动检查、1320309 项战斗检查、54 帧真实渲染通过；不是本轮公网双机实测 |
| `verify_effects.ps1` | 本轮通过严格编译、动态拖尾/烟雾/爆炸、HIT/碎心/受损纹理/红边及 W1/L1 实际渲染、双视角归属、首尾循环和返回门禁检查 |
| `verify_update_windows.ps1` | 未通过：1 项旧版夹具错误，状态测试提前停止；没有伪造后续独立更新窗口阶段通过 |
| 帮助一致性 | 18 段通过，简略 5 段、详细 13 段；文案同步不等于隐私法律/运营信息已经完整 |
| 备份工具 | 两项本地合成数据测试通过；未对线上执行备份/恢复 |
| 发行包嵌入组件 | 本轮核验客户端、隧道、升级器及素材哈希一致；成品仍未签名 |
| 线上只读检查 | 两项服务 active、NRestarts=0、readyz 正常；公网 TLS health 200、未认证后台 401；数据文件 0600，后端仅监听回环；磁盘使用 17% |
| 线上代码摘要 | 与 3.0.1 部署记录一致，未发现由另一项目改写这两项 PlanePet 程序的证据；不代表对其他项目开展审计 |

窗口报告：`dist/release-regression-fa5c40200d8e4d8babb0a632956164f5/中文 用户路径/检查结果.txt`。

战斗图像报告：`dist/battle-verification-95d9db07cbfa465eabe097ea79e5f6e2/`。

线上服务器 SHA-256：`b6191436b9793387f67fc9535d53c3d7c6a9c17e33bef5f9e32f208da739fbdd`。

线上统计网关 SHA-256：`4e498542fbcfdbc7757855c005e0fdf68c6424419b20eb3102618a4a5dba09ec`。

建议顺序：先处理 1、3、5 的已确认技术/文案问题，同时确定 4、6、7、10 所需运营方选择；完成 8 的实测与 9 的归档后，再执行 2 的正式发布。没有发布前扩大玩法、账号体系或商业功能的必要。
