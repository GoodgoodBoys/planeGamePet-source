# 历史战绩与对战状态栏：A 组经典像素爱心

`history-classic-a.png` 是用户选定的第一组原图的逐字节副本，未重新生成、涂改或另加背景。蓝心、红心、灰色空槽共用一张带真实透明通道的 2172 × 724 PNG。

- 来源：`../heart-concepts/2026-09-06/A-classic.png`。
- 生成工具：内置 imagegen（之前的素材选型阶段）；原始完整提示词见 `../heart-concepts/2026-09-06/README.md`。本次接入没有额外图像生成提示词。
- SHA256：`bbedfbb1ed22f3e11737f150f02952d069d97e3ed63f1503bfe01230cbcb7a64`。
- 资源号：桌面客户端 RCDATA 161。加载后保留 PNG 与内存流直到客户端结束，重复绘制不重复解码。
- 三帧采样区：`(70,58,632,568)`、`(770,58,632,568)`、`(1470,58,632,568)`，依次为蓝 / 红 / 空槽。原图留白在绘制时排除，不改写素材文件。
- 历史窗口尺寸：20 × 18 逻辑像素，中心沿既有血量列对齐，相邻中心距 21。历史窗口原有 DPI 缩放保持不变。
- 1.0.9 起也用于真实对战状态栏：13 × 12 像素，相邻中心距 14，仍在原有 240 × 52 状态栏内。蓝方从左向右排列剩余血量，红方镜像排列；双方各有 3 个固定心位，缺失血量显示同款灰槽。
- 两处共用一份内嵌图片、同一个缓存和最近邻采样函数，保留透明背景，不改血量或胜负计算。
- 资源或 GDI+ 加载失败时回退到已有矢量心形显示，避免血量不可见。

采样区由 `desktop/heart_assets.h` 定义，显示尺寸分别在 `desktop/history_layout.h` 和 `desktop/game_layout.h`。`tests/history_window_cases.h` 和 `tests/game_heart_window_cases.h` 检查资源 / 透明通道、DPI、双方视角 0–3 血量、凹口与间距、扣血重绘、边界和存档兼容。
