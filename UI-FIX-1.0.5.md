# 1.0.5 勿扰状态提示排版修复

2026-09-06：仅客户端排版修复，未操作 ECS、OSS、GitHub 或用户档案。旧版 1.0.4 包保留。

原因：提示卡顶部已改为 y=66，自身状态文字仍沿用 y=60 的旧矩形；同时采用自动换行标志，未按单行文本可靠垂直居中。

修改：

- 自身状态文字区域由实际提示卡边界推导，左右留 10px、上下留 8px，中心与提示卡一致。
- 字号 18→21，约增大 17%；开启、恢复接收、同步中和保存失败提示统一单行水平/垂直居中。
- 保留卡片尺寸、桌宠尺寸和位置；不修改好友勿扰倒计时/取消按钮、邀请和联网逻辑。

验证：

- `build.ps1 -BuildPublic` 成功，生成 1.0.5 单端/双端公网成品。
- 实际窗口 306 项通过。其中新增 64 项检查覆盖四种状态文字 × 四种 DPI（96/120/144/192），验证字宽字高、实际渲染像素居中、窗口位置不动，并已查看生成图片。
- 窗口报告及 PNG：`dist/release-regression-4282b1333e4c45939ba8d36280be90c6/中文 用户路径/`。
- 单端/双端 ZIP 静态检查及内嵌程序哈希核验通过，`git diff --check` 通过。此次未重复服务器、公网对战和升级事务测试，因为对应行为代码未修改。

最终 SHA256：

- 单端 EXE：`b9cca503c97f74f8e00a0e47a5502d2a2ab7495a523ac2bfbfa370d0619a1501`
- 单端 ZIP：`de65804a58c7500b50985fdf4fc74e27c4740151059b0135bf4e3f954ff274ae`
- 双端 ZIP：`b32c703d6157d45d1280888c2613e5bb755c84585179b2b851ecb0dcb0ba285b`

使用：退出旧程序，打开新包内的 PlanePet.exe；无需重新绑定。未向自动升级渠道发布，EXE 仍未附 Windows Authenticode 签名。
