# 1.0.13：表情展开记忆与 Idle 点击射击

2026-09-07。本次只调整桌宠交互、显示与帮助，不更改游戏规则、网络协议、匿名统计事件和存档格式。未修改 ECS、OSS、GitHub、FingerKnight 或真实用户数据。

## 完成的行为

- 展开表情栏后，鼠标移出仍在 0.4 秒后隐藏操作栏，但保留展开选择；再次移入直接显示四个表情。
- 从展开或最近一次有效发送表情起，10 分钟没有发送表情才自动收起。离线点击和发送冷却中被拦截的点击不延长时间。
- 隐藏桌宠、游戏及临时不可操作状态不立即清除展开选择，10 分钟计时继续；手动点击 ▶ 立即收起。重启程序默认收起，不新增存档项。
- Idle 蓝机不再自动发射子弹；红蓝飞机沿用原有跟随飞行，单机离线飞行也不变。
- 在桌宠空白处轻点左键，蓝机在松开时从当前机头发射一发子弹，朝鼠标方向飞行；超出范围时限制为机头左右各 30°。
- 子弹仅为本机视觉特效，无伤害、无击落，不发送联网动作或统计事件；实际对战仍按原规则自动射击。
- 点击按钮、飞机、表情气泡和提示框不射击。按住拖动、拖出后返回、释放在窗口外、捕获丢失、DPI 改变和隐藏均不会误射。
- 桌宠、游戏窗口、四个表情按钮及图标尺寸保持不变。帮助正文及包内说明已同步。

## 透明区与输入处理

原来的颜色透明区域会被 Windows 跳过，不能只增加鼠标点击判断。桌宠改用逐像素透明呈现：已有文字和模型保留原 RGB 像素，Idle 空白区使用最低非零 Alpha（1/255），因此接收点击而不点穿到后面的程序；其他状态空白区为零 Alpha，仍可穿透。没有全局鼠标钩子或额外后台线程。

此策略依据 [Microsoft 的分层窗口命中规则](https://learn.microsoft.com/en-us/windows/win32/winmsg/window-features)，并避免在同一 HWND 混用颜色透明与逐像素呈现（[API 限制](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setlayeredwindowattributes)）。已检查真实窗口合成图，没有可见实色背景块，文字和模型未重新缩放。

## 检查与测试

- `build.ps1 -BuildPublic` 成功，`-O2 -Wall -Wextra` 编译无警告。
- 原生 Windows 界面检查 **693 / 693 通过**。
  - 报告：`dist/release-regression-10357cc72b0a4a21af91df39d3e8f8ca/中文 用户路径/检查结果.txt`。
  - 包括真实 OS 鼠标输入：先验证目标像素属于隔离测试窗口，再注入一次点击并恢复鼠标；按下、松开均正确接收，准确产生一发子弹且释放捕获。
  - 四种 DPI（96 / 120 / 144 / 192）下真实空白命中、逐像素呈现和 280 × 150 窗口尺寸均通过；邀请状态透明空白仍穿透。
  - 10 分钟边界使用可控单调时钟覆盖隐藏、重新移入、发送刷新、离线 / 冷却不刷新和手动收起；不是单纯等待墙钟时间的手工测试。
  - 1825 组机头 / 鼠标方向组合验证 ±30° 限制、归一化速度和机头出生位置；正后方左右等距时允许取任一侧边界。
  - 子弹到期清理、最多 64 发的有界容器、零距离目标、拖动防误射、游戏切换清理、离线及隐藏绑定框的闲置状态通过。
  - 40 次真实分层呈现及原有重复绘制检查未增加 GDI 对象。
- 已读图检查闲置无子弹、点击后单发、真实桌面合成和简略帮助；文本无裁切，导航可见。
- 与 1.0.12 比较，四种 DPI 下四个表情图标区域逐像素一致；游戏和历史战绩共 8 张截图 SHA256 完全一致。
- `HELP.md` 与程序内五段简略正文、九段详细正文 **14 / 14 一致**；帮助测量和导航检查通过。
- `verify_fix_regressions.ps1` 最终完整通过：客户端 / 服务端边界、29 项网关与统计测试、隔离本地完整对战、匹配生命周期 / 恢复 / 解绑 / 版本兼容、勿扰与表情、升级安装 / 回滚 / 哈希错误 / 中断恢复、单双端公网包静态验证。
- `verify_pet_visual_assets.ps1` 通过：D2 飞机和云、43 帧勾引动画等既有资源与行为保护检查通过。
- `tests/verify_embedded_components.ps1 -Version 1.0.13` 通过：两个启动器内嵌组件正确，C 拳头及 A 组爱心与选定素材一致。
- `git diff --check` 通过；1.0.12 单端与双端 ZIP 保持原哈希，没有覆盖旧版本。

测试中修正了三处测试/说明问题：正后方方向的浮点左右二义性；真实 OS 点击测试需要处理激活等完整消息而非只筛选鼠标消息；双端包说明保留“四个表情”的完整提示。随界面用例增加，隔离测试进程上限由 20 秒调为 45 秒，正常最终运行全部完成。

## 交付文件

- 单端公网 EXE：`dist/PlanePet-Public-Single-1.0.13/PlanePet.exe`
  - SHA256：`bb958ba6b68b4d294bd207985d85d6527964cfda483595c99995ee4fb29f8c52`
- 单端公网 ZIP：`dist/PlanePet-Public-Single-1.0.13.zip`
  - SHA256：`8497b7556b52f236bce7e8835baceda06853cb2a150b8e88f3c127cca67ccb4a`
- 单机双端公网 ZIP：`dist/PlanePet-Public-DualLocal-1.0.13.zip`
  - SHA256：`87bda8e949292c7f39cd3698ff89ae4ffac925e9dae9b1a25297b93a5729b9b7`

先退出旧程序再启动新版。公网配置沿用原版；本轮未发布在线升级清单，未重复人工跨公网双人实战。EXE 仍未附 Authenticode 签名。以上验证有明确范围，不代表承诺所有环境绝无问题。
