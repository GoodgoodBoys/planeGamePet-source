# 3.0.2 对战特效交付与验收

日期：2026-09-08。范围：Windows PC → PC 客户端视觉呈现及结算返回；没有部署/修改 ECS、OSS、ESP32 或 FingerKnight，没有清理真实存档、统计或发布自动更新。

## 实现

- A 款双翼细拖尾：记录移动路径，历史点独立后移、淡出，转向时留下弯曲轨迹；不是与飞机一起平移的静态模型。
- B 风格详细烟雾：六个透明像素烟团变体，独立漂移、扩大、淡出，发射速度限制在机尾左右各 15°。2 血小而稀疏；1 血大而密集，同时拖尾变淡。
- 12 帧命中特效：0.48 秒；跟随受击飞机的接触偏移，与现有碰撞分数/呈现时间一致，去重且不提前播放未来命中。
- B 战败爆炸：12 帧、1.08 秒，火焰转烟雾；生命值归零的飞机在闪光后隐藏。
- WIN!/LOST! 收到结算即显示，不等待动画。提前返回记住一次操作，播完再执行。对方先返回也不会截断本端动画。平局、断线、同步中止不伪造击毁爆炸。紧急隐藏及退出仍可立即执行。
- 原有 280×150 桌宠、240×372 对战窗口（52px HUD + 320px 地图）、模型和字体尺寸不变。详细帮助已同步说明受损效果及结算返回。
- appVersion 为 3.0.2，仍是 v3 兼容系列；没有修改线上协议、碰撞判定、速度、战绩或统计格式。

## 已执行检查

| 检查 | 结果 |
| --- | --- |
| C++17 `-Wall -Wextra -Werror` 特效模型及真实客户端绘制编译 | 通过 |
| `build.ps1 -BuildPublic` | 全量编译及单端/双端公网包构建通过 |
| `verify_effects.ps1` | 通过；204 张真实客户端渲染帧，30 个嵌入透明资源帧 |
| 特效时钟、角度、帧率、资源边界 | 30/60/144 FPS 发射计数一致；2 血 10 秒 37 团，1 血 108 团，初始平均尺寸 6.88/11.40px；±15°、生命周期、对象数量上限、重复事件和下一局复位均通过 |
| 结算与返回 | 立即结果、完整播放、本端提前返回、对方提前返回、无服务器响应的排队返回、异常结束无爆炸通过 |
| 状态栏隔离 | 两端地图边缘有/无特效，顶部 52px HUD 逐字节一致 |
| 绘制与视觉检查 | 两端健康/受损/命中/爆炸图像人工读图；透明底无品红矩形；GDI 对象稳定；200 帧渲染加 BMP 保存均值约 5.8ms（本机测量，不是跨设备性能保证） |
| `verify_battle.ps1` | 移动核心 777476 检查、战斗核心 1320309 检查；真实绘制接触及最终权威状态 54 帧检查通过 |
| 隔离本机真实双端联机 | 200ms 模拟 RTT/2% 丢包对局通过；300ms/5% 丢包明确同步中止通过；对战和倒计时各 7 秒全断网后恢复通过，无错误新增战绩 |
| `verify_fix_regressions.ps1` | 客户端/服务器状态、35 个网关统计测试、绑定生命周期、版本兼容、邀请、勿扰、表情、布局、战绩及升级安装/回滚/中断恢复通过 |
| `verify_game_render.ps1` | 游戏边界、地图对称、桌宠模型/云/追击连续性通过，窗口尺寸保持原值 |
| `verify_release_hardening.ps1` | 正常 Windows 桌面环境 702 项通过、0 项失败；包含帮助、历史、菜单、悬停、托盘、DPI、弹窗、紧急隐藏等 |
| 单端与双端发布包静态检查 | 通过；版本 3.0.2、x64 GUI、图标、内嵌组件、归档内容、帮助文档与新编译文件哈希一致 |
| `git diff --check`（修改的既有文件） | 通过；只有已有 LF/CRLF 提示，无空白错误 |

窗口验收第一次在沙箱内有两项托盘和一项真实鼠标悬停失败；同代码在正常桌面环境重跑全部通过，未因此修改生产逻辑。

## 文件与复核入口

- `desktop/game_effects.h`：纯视觉状态/粒子模型。
- `desktop/main.cpp`：事件对接、真实绘制、结算保留与返回流程。
- `assets/effects/README.md`：全部最终素材路径、内置 imagegen 模式、完整实际提示词和可复现导入说明。
- `tests/game_effects_test.cpp`、`tests/game_effects_render_test.cpp`、`verify_effects.ps1`：新增验收。
- `dist/vfx-verification-3.0.2/actual-game-effects.gif`：真实绘制函数生成的双视角示范，不是公网对局录像。
- `dist/release-regression-4b3ec3bf4d5c4d6995cd86898b4ed743/中文 用户路径/检查结果.txt`：702 项完整窗口验收记录与同目录图像。
- `dist/battle-verification-478b2638f9d04080a6bce4cac791154b`：真实同步呈现检查图像。

## 交付包

- 单端：`dist/PlanePet-Public-Single-3.0.2.zip`；解压运行 `PlanePet.exe`。
- 同机双端：`dist/PlanePet-Public-DualLocal-3.0.2.zip`；解压运行 `PlanePet.exe`。沿用原有公网 WSS 地址与双端隔离身份配置。
- 单端 SHA256：`4e7a589f01428213eac77a8d3cdbe1038ebb4fd37fd2f0b683f71362a2253f88`。
- 双端 SHA256：`bc017607748e56259da68fe6dc4569f4714446a5ea9a90aa3e8aa1d913566895`。

本轮真实联机验收使用隔离本机服务器，没有另行执行公网双端完整对局。公网包已通过静态完整性检查；外网可达性仍取决于实际网络及现有服务器状态。EXE 沿用未签名的测试发行方式，不能视为已具备代码签名的正式商用发行包。自动化检查及读图没有发现本次改动引入的回归，但不能证明任何环境下绝无缺陷。
