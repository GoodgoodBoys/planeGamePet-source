# Plane Pet 3.0.1 — 三项定向修复与验收

日期：2026-09-08。状态：三项修复、构建、本地与公网自动验收完成；Plane Pet 服务端及统计后台已部署。单端与单机双端公网包均已生成；未推送 GitHub 或 OSS 稳定更新清单。不是对整个应用所有环境“无 bug”的承诺。

## 修复范围

1. **结算画面**：Finished 只显示服务器快照；丢弃未确认预测步，即使本机此前已发生同步故障，也能正确接收最终状态。正常对战仍使用整局预测和原有限速，不改变地图、模型、碰撞或操作。
2. **断网战绩**：5 秒收不到消息不再调用主动弃局记负逻辑，也不提前写入本局去重标记；迟到权威结算仍可正常处理一次。服务端 v3 倒数/对战被动掉线或进程恢复中断按同步中止处理；收到明确 Goodbye 时保留原主动退出规则。已确认同步中止的对局不因退出重复记负。不改历史文件格式，不自动改写旧错误战绩。
3. **匿名统计**：正常结束与异常中止、退出／掉线结束分开计算；平均正常对局时长允许最多 182 秒的实际用时，涵盖 180 秒模拟与有限确认等待。旧版缺少原因的记录保留原口径并标明不确定性，新版漏报原因单列待确认。双方重复上报去重，迟到权威结果优先于本地断线推测。不改 SQLite 结构、授权流程或原始事件。

大版本仍为 3，未修改协议消息格式；两端都使用 3.0.1 才能获得本次客户端修复。ESP32/shared 原文件未修改，旧场景集成回归通过。

## 验收结果与证据

- Windows 全量 `build.ps1 -BuildPublic` 通过，单端与双端 EXE 版本为 3.0.1。
- Windows 客户端、服务端定向测试通过 `-Wall -Wextra -Werror` 编译与执行；包含最终状态迟到、本机曾发生预测故障、双方视角血量与战绩、断网去重、迟到结果、主动退出、倒数及对战被动超时。
- 3.x 核心 1,320,309 次断言通过；2.x 核心 777,476 次断言通过。覆盖限速、镜像边界、连续碰撞、网络丢包与抖动、非对称延迟、完整重演和 180 秒上限。
- Linux 实际编译服务端和状态测试，严格警告检查通过；核心 UBSan 检查通过。
- Windows/Linux 均通过 35 项网关与统计测试；含中止不计完成/平局、180.2 秒有效时长、异常数值、先断线后迟到结算、双端重复事件、旧数据读取不改原始行等。
- 真实渲染器产生 54 张图。6 个结算场景分别与仅使用服务器快照的参考图逐像素相同；已直接查看 WIN、LOST、同步中止的代表图，爱心、结果与提示正确。目录：`dist/battle-verification-9bfb5c51edd14a8794bf7bf83189a723`。
- 本地真实双客户端：RTT 200ms、2% 丢包正常对局；RTT 300ms、5% 丢包同步中止。报告：`dist/motion-live-122c7c3f1f1742d4a7d5048e30b8eee3`、`dist/motion-live-822e37ee1cf24acdbe766cc3941ddf0c`。
- 本地完整断流：倒数和对战中分别将 A 的全部双向数据丢弃 7 秒，再恢复；两端无误记胜负。报告：`dist/motion-live-b5768445e18348e680fe8c5bb10f33da`、`dist/motion-live-2c38f6823f3942c186bb7829c7ac2d42`。
- 公网真实客户端经现有 WSS 连接：正常对战、倒数断网恢复、对战断网恢复全部通过。报告：`dist/motion-live-4904d326b23548e79b82c5d93bdadbeb`、`dist/motion-live-621c36a20f044484ba912d1df3566534`、`dist/motion-live-8910571f790b48ca87bed5a0185c2c2b`。两次最终断网复验双方均收到权威中止，均无新增胜负记录。
- `verify_fix_regressions.ps1` 通过：绑定生命周期/恢复/解绑、版本兼容、勿扰与表情、旧场景完整对局、布局与镜像、升级安装/失败回滚/中断恢复、单端和双端交付包静态检查。
- 同一窗口测试 EXE 在真实桌面通过 **702/702**。初次隔离运行的两项托盘检查因没有可用 Explorer 托盘失败，未改代码、未跳过检查，切换到真实桌面后复验通过。报告：`dist/release-regression-a7ad2240aa5b42e09769ef682efa09c4/桌面复验-212fc117ce0744c5b28d83d95d9b3ac6/report.txt`。
- 单端/双端内嵌组件摘要一致；爱心和拳头资源校验通过；18 个帮助段落同步校验、Python 语法及 `git diff --check` 通过。

### 测试判据修正记录

断网恢复验收不要求结算窗口持续停留：在线方可能先返回桌宠，断网方恢复时服务器已回到菜单，不能要求强行重开旧局或重放已返回的结算。本轮一项初始公网测试因此失败；轨迹显示在线方已收到中止、断网方本地提示连接中断，双方均未误记战绩并恢复联网。测试已改为验证这些实际要求，同时仍严格要求在线方收到权威中止；所有实际收到的终局都不得包含胜者或正常结果。恢复前后战绩必须保持不变。对迟到终局能够收到的路径，另有去重和最终结果定向测试。

## 公网部署与保护

- 仅替换 `/opt/plane-pet/bin/plane-pet-server` 与 `/opt/plane-pet/gateway/plane_pet_gateway.py`，仅重启 `plane-pet.service`、`plane-pet-gateway.service`。切换前确认无现有在线连接。
- 备份：`/opt/plane-pet/backups/plane-pet-20260908T013516Z-7cbe1a9e`。包含原代码、绑定、网关凭据、SQLite 在线备份及受保护文件摘要；保留，不删除。
- FingerKnight、旧设备服务、Nginx 的服务状态/PID与受保护文件摘要核对一致，既有配置未修改。
- 绑定/凭据摘要保持一致；现有统计副本已用新后台验证。部署后管理页 HTTP 200，新增统计分类存在，数据库完整性通过；只读核查时备份和线上均为 3,865 条事件。测试关闭统计，未登记新公网凭据，合成绑定和本地 QA 凭据副本已清理。
- Linux 服务端 SHA-256：`b6191436b9793387f67fc9535d53c3d7c6a9c17e33bef5f9e32f208da739fbdd`。
- 统计网关 SHA-256：`4e498542fbcfdbc7757855c005e0fdf68c6424419b20eb3102618a4a5dba09ec`。

## 交付包

- `dist/PlanePet-Public-Single-3.0.1/PlanePet.exe`：一台电脑一个客户端，沿用原单端资料。
- `dist/PlanePet-Public-Single-3.0.1.zip`：SHA-256 `53ca76c7c0fdb442fbec3a92dc4e895242aea59b665dbf34d46f19be9460bc41`。
- `dist/PlanePet-Public-DualLocal-3.0.1/PlanePet.exe`：一台电脑两个客户端，经公网服务器连接。
- `dist/PlanePet-Public-DualLocal-3.0.1.zip`：SHA-256 `b81b271c7d8a6d19f8f86022181d510cf6cbfac619da0662054d26aef6ef127d`。
- 内嵌客户端 SHA-256：`463e3b1df8545c7cbb84872dbbe28cb58b7387ee5a410b73bdd41925a26ce09a`。

仍属测试候选包，未增加商业代码签名；实际两台独立电脑与人工鼠标体验、正式发布清单、长期运行及其他发布准备仍按上轮审查清单另行验收。本轮未扩大到这些事项。
