# 1.0.3 候选版修复与验收结果

日期：2026-09-06。状态：候选版，不是已完成正式发布验收的稳定版。

## 最新追补：邀请卡片按钮边距

- 已复现布局原因：原接收卡片与接受/拒绝按钮底边均为 `y=122`；原等待卡片底边为 `y=122`，取消按钮底边为 `y=121`。
- 接收卡片改为 `196×72`、按钮 `82×28`、按钮间隔 12 像素；等待卡片改为 `244×56`、取消按钮 `48×36`。按钮与卡片外边框至少间隔 10 像素，标题、倒计时、按钮互不重叠。文字大小未改变。
- 整体 HWND 仍为 `280×150`，不因邀请改变窗口位置或飞机动画轨迹。等待期间收起原本不可发送的表情栏，回到空闲自动恢复。拒绝/超时反馈卡底边收至 110，与恢复后的表情栏相隔 6 像素；新邀请优先于残留的旧邀请反馈。
- `desktop/pet_layout.h` 为绘制、点击、拖动命中和回归提供同一份坐标，静态断言保护边距与间隔。新增真实客户端按钮边缘点击、按钮间隙/外侧留白不提交动作、表情栏隐藏与恢复测试，均通过。
- 窗口回归共 130 项通过，其中新增 44 项邀请布局检查：GDI 实际字体测量、完整标题/三个点/`300s`、100/125/150/200% 测试 DPI、300/99/0 秒生成图、固定窗口位置大小、卡片留白可拖动及表情可见性。人工查看接收卡、300 秒等待卡、200% 测试 DPI 下 0 秒卡和空闲图；边距清楚、文字无裁切。不是实体多屏人工验收。
- `build.ps1 -BuildPublic`、完整功能回归、更新状态/窗口回归、单/双端包静态检查及内嵌组件 SHA 一致性检查重新通过。本轮只改桌宠卡片布局、测试和说明，未改游戏规则、联网/统计协议、用户数据、ECS、FingerKnight、ESP32、OSS 或 GitHub。

## 前轮追补：右键菜单分组

- 按用户确认方案，常态一级七项：邀请对战、历史战绩、暂停接收邀请、隐藏悬浮窗、设置与隐私、帮助与更新、退出。设置组保留统计开关、导出统计、清除本机战绩/统计、解绑；帮助组保留帮助、检查更新、诊断和诊断导出。最多两层，未删功能。
- 未绑定时用勾选式绑定框入口替代无效邀请；搜索时同时保留停止匹配与绑定框开关；好友离线时标注原因。桌宠与托盘共用一个构建函数；隐藏后改为显示入口，游戏隐藏后恢复游戏窗口。原有统计同意、清除/解绑确认及双端版更新禁用状态保留。
- 原生菜单在展开期间保持项目稳定。子菜单退出、模态计数归零后才分发命令；取消返回 0 不执行命令；子菜单跟随父菜单递归释放，创建失败也清理句柄。
- 新增 28 项菜单回归，总计 86 项窗口检查通过；包含原生 HMENU 顺序/层级/文案/勾选/禁用状态、帮助/历史/诊断及隐藏恢复命令路径。连续构建销毁 300 次，测试内 USER 句柄无增量、统计同意状态不变。此项不是长期句柄泄漏证明，也不替代真实鼠标悬停/屏幕边缘展开的人工体验。
- 更新状态与窗口测试改用子菜单相同的命令分发入口，重新检查、关闭与迟到结果回归通过。帮助页、隐私文件、README、单/双端及离线包说明同步，已查看帮助页尾生成图，无裁切。
- 本轮只调整客户端菜单/文案、测试及打包说明；未改窗口大小、字体、游戏规则、网络/统计协议或系统/服务器配置，未推送 GitHub/OSS。下面保留前轮公网延迟、真实升级及服务端验证证据，不冒充为本轮重新测量。

## 前轮追补：高延迟与尺寸回退

本节和下方新摘要对应重新打包的 1.0.3 候选包，未发布到 OSS stable 或 GitHub。

- 原因 1：游戏隧道从 `NO_PROXY` 改为 `AUTOMATIC_PROXY` 后继承实际用户启用的本机 HTTP 代理。相同服务器、同机双隧道对照：旧 1.0.2 中位 46/46ms，问题版 734/875ms（P95 1188/1328ms）。仅恢复游戏直连后 20 秒中位 47/47ms，640/640 回包；存在短时网络抖动，P95 140/141ms，不承诺恒定低延迟。
- 真实客户端验证：使用真实游戏 UI 定时消息泵、两条 WinHTTP/WSS 隧道和生产 PlanePet 服务器；两端自动向各自左边移动以延长采样。最后一轮各 12 次 Playing 状态 RTT，中位 62/54.5ms、P95 和最大值均 78ms，HUD 最后显示 60/57ms。匹配、300 秒邀请、取消、表情、倒数、对局、重启结束原因和单次战绩均通过。前一轮中位 61.5/54ms、最高 93ms，同样通过。
- 原因 2：上轮将紧凑窗口尺寸及绘图乘以显示缩放，改变了原先体积。现恢复桌宠 280×150、游戏客户区 240×372（HUD 52、地图 320）、更新卡 360×244 物理像素；内容与字体不额外放大。保留清晰 DPI 感知、GDI/GDI+ 坐标一致性、点击区域、拖动和工作区夹取；原生边框按真实 HWND DPI 计算，帮助/历史沿用原策略。
- 窗口回归从 54 增至 58 项；新加真实游戏客户区尺寸断言，修复注入 DPI 与真实窗口标题栏指标混用。96/120/144/192 消息测试全部通过；真实公网双端实际尺寸也为 240×372。已查看紧凑桌宠、游戏 HUD/地图、诊断生成图。
- 仅修改本机客户端、隧道、测试和文档；没有修改 ECS 程序/配置、系统代理、FingerKnight、ESP32、Nginx 或 OSS 清单。公网回归复用现有 QA 加密凭据副本，关闭统计上传；仅创建合成配对并在结束后解绑，原用户档案哈希未变，临时凭据副本已删除。
- 隧道原始对照：`dist/latency-probe-d061ae15c9ff4fbd9100f99618cb93e3/results.json`；直连修复对照：`dist/latency-probe-4ada82ec532341b3996c3301e34a1f8d/results.json`。最终隧道用高精度计时器、20 次/秒复测 12 秒，480/480 回包，中位 38.77/41.39ms、P95 54.19/57.23ms：`dist/latency-probe-328890f605a54f1d9d116c640e192cf8/results.json`。最终实际游戏：`dist/public-online-check-887d160218ff/gameplay-latency.json`。首轮未移动测试约 3.4 秒正常结束，未达到 12 个游戏样本；加入仅测试构建自动移动后复测通过，未改变正式游戏规则。

游戏直连与更新下载是分开的：更新下载继续使用系统代理。受限网络是否允许直连仍须单独验收，不修改或绕过组织网络限制。

## 1～4 复核结论

| 项目 | 修复 | 实际验证 |
|---|---|---|
| 中文路径/参数乱码 | Windows Unicode 参数解析、严格参数边界和可靠引号转义 | 真实中文/空格路径存档、中文名称、引号和尾部反斜线回归通过 |
| 升级失败与过早确认成功 | 启动失败专用可见状态；新版初始化并持续泵送 UI 后发就绪信号；健康记录后再报成功统计；保留上一版救援副本 | 状态与事务回归通过；真实 1.0.2 旧升级器→最终 1.0.3 启动器/客户端/隧道成功，中文隔离档案保留、公网 TLS 通过；未改线上稳定清单 |
| 邀请接受率可超过 100% | 分子与分母使用同一发送 ID 集合；漏报结果单列，不盲目截断比率 | 重复、乱序、缺失、90 天过期窗口回归通过；Linux 上现有 1712 条数据副本新旧读取兼容 |
| 桌宠隐藏后难以找回 | Explorer 重建通知恢复托盘；二次启动激活公开单端实例；快捷键冲突可见 | 用户桌面的独立窗口托盘/重复通知/指定实例激活/不误唤其他进程通过；未真实重启用户 Explorer |

第 3 项新网关已于 2026-09-06 上线，线上统计使用新口径。第 4 项的新激活协议不能改造已经运行中的旧版进程。

## 本轮已补充

- 游戏直连公网、分类网络状态、退避及 Retry-After；手动脱敏诊断导出；更新下载保留系统代理。
- 容量/拒绝原因/统计写失败及丢弃指标、仅本机就绪检查。
- 待机消息泵和绘制节流；主悬浮窗、游戏、更新卡和信息窗口 DPI 处理，绘制与点击共用逻辑坐标。
- 代码签名构建流程及无证书时的正式发布阻断检查。
- 白名单数据备份、完整性验证、独立运维与发布救援说明。
- 帮助、隐私和统计口径同步；打包/内嵌第三方与隐私说明。

## 已完成验证

1. `build.ps1 -BuildPublic`：Windows C++17/O2/Wall/Wextra 完整编译，单/双公网候选包生成成功。
2. `verify_fix_regressions.ps1`：最终完整回归通过。覆盖配对等待/取消/断网清理/持久恢复/解绑/码复用/版本兼容、邀请 300 秒、双方对局和结束返回、扣血不复位、三分钟平局、表情、反重放、统计、升级事务及包结构。
3. `verify_update_windows.ps1`：更新状态和窗口交互回归通过，覆盖关闭按钮、迟到请求、重新检查、邀请/对战期间延迟展示、不在游戏中安装等情况。
4. `verify_release_hardening.ps1`：130 项窗口/菜单/邀请布局/中文路径/托盘/就绪/诊断/DPI 回归通过；四档测试保持紧凑尺寸，覆盖按钮边距与命中、表情栏恢复、更新关闭、完整地图坐标及结算返回。测试不改变 Windows 显示设置、不重启 Explorer。
5. 游戏边界和桌宠资源测试通过；人工查看游戏 HUD/地图边界、在线双机桌宠、帮助页尾及诊断页面生成图，无该测试分辨率下的遮挡/溢出。不是全 DPI/实体多屏验收。
6. Linux 暂存：Ubuntu GCC 11.4、Python 3.10.12 编译服务端、状态测试、26 项网关/统计测试、服务端边界/只读就绪探针通过；Windows/Linux 各 2 项备份测试通过。
7. 统计兼容：ECS 专属目录中读取生产数据副本，新旧网关各保留 1712/1712 条记录，表结构与凭据不变；没有修改或清空生产统计，没有把生产数据下载到本机。
8. 公网冒烟：新隧道使用既有 DPAPI 凭据，`--enroll=0`、独立端口和本机临时配置，通过现有 WSS/TLS 入口；不玩游戏、不上传统计、不登记新身份。临时凭据副本已清除，原凭据保留。
9. PE 文件/产品版本为 1.0.3；单/双端 ZIP 各 18 个文件。候选包为 NotSigned；未提供证书的 `-RequireSignature` 构建按预期失败。
10. Git 空白及 PowerShell/Python 静态语法检查通过，无推送、提交或 OSS stable 改写。
11. 前轮尺寸回退版真实升级报告：`dist/candidate-upgrade-2637f686344349ee853e53609ce82be2/`。升级器直接从 1.0.2 发布 EXE 提取；实际新启动器提取并启动组件、健康提交、公网 TLS、合成档案保留均通过；原用户档案哈希未改变，临时凭据已清除。这不是修改线上稳定清单后的检测/下载全流程；本轮菜单调整另行复测升级事务与内嵌组件一致性。
12. 本地容量：128 条 WebSocket/UDP 隧道、额外连接明确拒绝、3 波 48 次重连、2944 次回环通过；RTT 中位 11.52ms/P95 14.73ms，测试结束连接计数归零。仅本机明文 WebSocket+UDP echo，不包括 TLS/Nginx/业务模拟器，不能推导 ECS 正式容量。
13. 生产部署：仅替换 PlanePet 服务端/网关程序并短暂重启两项服务；部署后 1878 条历史记录、绑定与凭据保留，表结构未变。就绪 200、公网 TLS 200、未认证后台 401、认证后台 200。Nginx 全部文件、两项服务配置、FingerKnight 主程序文件摘要，以及 FingerKnight/ESP32/Nginx 服务 PID/状态前后一致。
14. 备份恢复演练：复制此次停服备份到暂存目录，独立回环端口启动；3 对绑定、6 个端点恢复，14 份网关凭据加载，后台可读。原始备份重新校验通过，临时恢复副本清除，生产数据未修改。

## 短时性能对照

同机、2 秒预热、新旧各 12 秒采样，场景相同。CPU 是单核百分比，不是整机百分比；内存为工作集，句柄是进程句柄，不等同于 GDI 专项统计。

| 场景 | 1.0.2 单核 CPU | 1.0.3 单核 CPU | 1.0.2 / 1.0.3 内存 |
|---|---:|---:|---:|
| 可见、未绑定、显示配对卡 | 48.53% | 10.14% | 19.2 / 18.9 MiB |
| 隐藏、未绑定 | 0.26% | 采样精度下 0.00% | 16.8 / 17.0 MiB |

表中为尺寸回退前 DPI 候选版的历史短测，可见场景两版句柄增量均 +2，隐藏均 0。较早一轮初改版对比为 36.30%→8.07%；不是当前紧凑候选包的重新性能基准。消息泵和隐藏绘制节流保留，仍需长时间追击/对局、GDI 和多屏验收。

## 候选包及校验

目录：`dist/PlanePet-Public-Single-1.0.3/`，解压分发包：`dist/PlanePet-Public-Single-1.0.3.zip`。

| 文件 | SHA-256 |
|---|---|
| 单端 `PlanePet.exe` | `bba5995f9028f3217d4b7377f44a3538544644d41f2e0a03899597b2812a5640` |
| 单端 ZIP | `255903f9f6877bc9fbd6f735c07ba557cfb0de5e0d0b32f241e85f465fa29d73` |
| 双端 ZIP | `94620e9665576b4fb08a1ba6e507c5c57b194cc8256e4dc39ce361d0710aef40` |

`tests/verify_embedded_components.ps1` 已逐项核对两个 EXE 中内嵌的隧道、客户端，以及单端升级器与本次构建 SHA-256 一致，避免旧联网模块残留在包内。

Linux 暂存路径：`/opt/plane-pet/staging/hardening-1.0.3-20260906/source-complete/linux-check.2EooHkHF`。
候选服务端 SHA-256：`abd3d3fe4526996882b6a68bb53e93991cc67ac494783555f0aeefa2eace6e19`。
候选网关 SHA-256：`07e81700fadb10d0014ca81229fa9ac3981a02453ad2058fb8bb3702d8b340c4`。

最终本机性能结果：`dist/idle-benchmark-39a2d31eb28847ccb3d4b97814626733/results.json`；初改版对照保留于 `dist/idle-benchmark-b1e92699a42b4c0090c87727daf75c4a/results.json`。
最后窗口/菜单/邀请布局/中文路径/DPI 报告：`dist/release-regression-cb6fa95941224eb4a176318dc8a90086/中文 用户路径/检查结果.txt`。同目录保留四档紧凑 `game-<dpi>.png`、`pet-<dpi>.png`、`invite-incoming-<dpi>.png`、`invite-outgoing-<seconds>-<dpi>.png` 和帮助页尾图。更新窗口报告：`%LOCALAPPDATA%/Temp/plane-pet-update-window-06e494933a6646719624e7d85aa29e2a/window-test.txt`。

生产代码备份：`/opt/plane-pet/backups/hardening-1.0.3-20260906.tYCbg2ed/`；数据备份在其 `data/plane-pet-20260906T070856Z-3243d6a3/`。部署成功标记：`/opt/plane-pet/staging/hardening-1.0.3-20260906/ACTIVATION_OK`。不要重复运行固定基线部署脚本。

## 仍未完成，不应误标通过

- OSS stable 指向 1.0.3 后的“检测→下载→安装”正式推广验收；当前刻意保持稳定清单不变。安装启动链的真实过渡升级已通过。
- 有效发布者证书及最终签名成品、专用域名/443 迁移、实际认证代理和安全软件验收。
- 真实混合 DPI 多屏拖动/拔屏与极小工作区、实体双机/不同网络和跨卷升级、生产完整链目标规模压测、长时间稳定性。DPI 代码/四档自动化与本地容量回归已经完成。
- 告警接收方与持续监控、加密异机备份、更新公钥轮换协议。实际隔离备份恢复启动已经通过。
- 发布者/联系方式/素材与源码许可决定、远端数据删除申请流程。

这些项目的依赖、风险、实施顺序及验收条件见 `RELEASE-HARDENING-PLAN.md`；没有用“无 bug”替代未执行的验证。
