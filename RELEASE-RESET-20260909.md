# 1.0.0 基线 → 1.0.1 更新验证

本次按开发者明确要求，对当前完整功能版本重新编号。1.0.0 是安装起点；1.0.1 是同一套功能的升级验证版，仅提升修订号和版本说明。两版均属于 `release_epoch=1`、`release` 通道，使用相同的游戏协议、更新验签公钥和数据目录，最低支持版本为 1.0.0，属于可选更新。

保留当前飞机/云/表情/受击素材、帮助与关于、勿扰、绑定与战绩、同步机制和已确认修复。不会为测试升级修改真实绑定、设置、战绩或统计。

准备状态：正在构建与验收。实际下载地址、SHA-256、Git 提交/标签及云端清理清单在发布验收后补入，本文不代表已发布成功。

## 发布与清理范围

仅处理 PlanePet 的公开发行文件和更新清单；最终保留新基线 1.0.0 和更新目标 1.0.1。Bucket 内其他项目、ESP32 固件、权限配置不变，ECS 运行服务、用户数据和救援备份不清理。

旧 Git 提交和标签保留，不强制覆盖历史。旧测试更新通道清理后，历史测试版需手动下载本次 1.0.0；旧数字更大的测试版本不会自动降级。

## 用户测试

1. 从旧程序菜单选择“退出”，不要只隐藏桌宠。
2. 下载本次 1.0.0，运行 `PlanePet.exe`，不要删除 `%LOCALAPPDATA%\PlanePet`。
3. 在右键菜单“帮助与更新 → 检查软件更新…”确认检测到 1.0.1，选择升级。
4. 等待下载、验签和重新启动；关于页应显示 1.0.1，绑定、设置和战绩应保留。
5. 再检查更新，应提示已是最新版本。

此前拒绝更新的七天静默可能抑制自动弹出，但手动检查不受影响。两个制品暂未有 Windows 发布者签名，更新清单仍使用原有 RSA 签名及 SHA-256 检验，不关闭任何更新安全校验。
