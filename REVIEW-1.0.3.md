# 1.0.3：原追逐回退及四项确认问题修复

日期：2026-09-09。状态：本地候选包已构建、验证；未上传 GitHub / OSS，未改 ECS、FingerKnight 或真实用户档案。保留之前 1.0.1 / 1.0.2 候选包，供对照。

## 本轮范围与修复

1. **Idle 飞行回退**：`PetFormationAt` 与原始 `1659984` 基线逐字一致，恢复总体向右、红机右前/蓝机左后、原平滑转弯。撤销绕圈与随机全方向实验逻辑及对应测试；保留最新同排“开一局 + 4 表情”按钮和 280×150 窗口，未改真实对战、飞机素材、自动射击 ±15°、勿扰/在线状态。
2. **战绩保护**：独立解析器限制读取大小，按完整记录逐行校验，拒绝空文件、非法数字/血量、计数不符、缺行及 EOF 半行。正常保存先更新有效 `.bak`，再原子替换主文件；写临时文件后 FlushFileBuffers。启动异常时先另存损坏原档再恢复有效备份；不能安全恢复则阻止后续覆盖，在桌宠、历史窗口和诊断中提示，本次新增暂留内存。保存前再次校验主档，保护运行中损坏的文件。显式清理也清理备份/损坏副本，防止重启恢复已清除数据。
3. **统计版本兼容**：新 CSV 字段为 `schema_version,release_epoch,timestamp_ms,client_id,app_version,invite_id,round_id,event,value`。当前新行为 schema=2 / epoch=1。旧七列 CSV 原样另存，导出前缀为 `1,unknown`；不猜测旧版本代际。游戏、服务器及上传统计协议保持不变。战绩仍写既有 v2 格式，并可读取 v1，避免升级回滚后的旧客户端无法识别。
4. **后台有限日志**：日常 LogEvent 只构建字符串、入队，文件检查/迁移/追加/轮转在单独线程。最多 512 个排队事件、4×1 MiB 滚动日志；满队列和写盘失败计数可在诊断中查看。导出、清理按队列顺序执行，导出禁止覆盖日志源；清理后不会由此前排队的事件重新生成旧记录。停止写盘最多等待 1.5 秒，极端磁盘阻塞不阻止进程退出，工作线程仅持有自己的共享状态，不引用销毁后的客户端。
5. **更新取消**：将同步 WinHTTP 改为异步请求 + 25ms 可取消等待。发送、响应头、读响应体都可取消；应用退出时提前取消。请求只由执行该请求的工作线程在 API 返回后关闭；待完成读取使用堆缓冲区，WinHTTP 最终 HANDLE_CLOSING 回调负责释放回调引用。保持 HTTPS、主机白名单、禁止重定向、签名、哈希和大小限制。

WinHTTP 生命周期实现依据微软的[并发规则](https://learn.microsoft.com/en-us/windows/win32/winhttp/concurrency-in-winhttp)与[回调生命周期说明](https://learn.microsoft.com/en-us/windows/win32/api/winhttp/nc-winhttp-winhttp_status_callback)，不从另一线程强行关闭正在执行的同步请求句柄。

## 验收结果

- 两次完整 `build.ps1 -BuildPublic`，最终构建无编译 warning/error。
- 存储回归 **406 项**：原损坏复现、下一局不覆盖、有效备份恢复/保留坏档、运行中损坏、锁文件保存失败、清除后不复活、版本兼容、拒绝/关闭统计后不记录、旧 CSV 保留与 unknown 标注、轮转、导出、防源覆盖、队列拥塞、清理顺序。故意让后台写盘延迟时，10000 次入队调用合计 **1ms**（本机实测），满队列可观察地丢弃可选统计。
- 正常 Windows 环境网络回归 **22 项**：响应头阻塞、响应体阻塞、TLS 握手阻塞各重复 6 次，交替显式取消/析构取消；成功读取、超限、重定向和不完整响应拒绝。最终轮最长取消析构 **32ms**，最终存活请求上下文 **0**。沙箱无法初始化 TLS，故该项在获准的正常 Windows 环境执行，仅连接 127.0.0.1。
- 更新状态/原生窗口 **76 PASS**：关闭、Esc、迟到结果、再次检查、下载/安装取消、邀请/游戏优先等旧行为保留。
- 完整原生窗口 **787 PASS / 0 FAIL**：右键、拖动、按钮/邀请、DPI、战绩、帮助、关于和隐私；新增三张战绩异常/恢复图，已逐图查看；已查看回退后的在线双机图。
- 原追逐 **4×120 秒**：全部向右，中心距离约 **104–123px**，16ms 最大位移 <0.31px，均在窗内，保留左右向转弯。
- `verify_fix_regressions.ps1` 完整通过，包括 38 项网关测试、配对/持久绑定/解绑/版本兼容、战绩旧格式迁移、勿扰、客户端移动/战斗、更新安装/失败回滚/中断恢复。
- `verify_battle.ps1` 完整通过：移动核心 777476、战斗核心 1320309 项；实际渲染 54 帧；本机双端 200ms/2% 丢包正常对局、300ms/5% 的明确中止、Playing/Countdown 各 7 秒完全断网恢复，两端历史未被中止误记。
- 帮助 18 节同步校验、图像资源校验、11 项发布前置检查、最终单/双端压缩包与内嵌组件/隐私/许可/特效哈希一致性检查通过。
- 新版生产 HTTPS + RSA 只读校验现有 OSS 旧测试清单及签名通过（616 字节，未安装）。正式 release/latest.json 仍为 **HTTP 404 NoSuchKey**，是尚未发布的外部状态；旧 stable/latest.json 仍为历史 1.0.2，不应混入正式 release/epoch=1 通道。

证据日志位于 `dist/build-1.0.3.log`、`dist/regressions-1.0.3.log`、`dist/update-1.0.3.log`、`dist/update-https-1.0.3.log`、`dist/windows-1.0.3.log`、`dist/render-1.0.3.log`、`dist/battle-1.0.3.log`。

原生截图目录：`dist/release-regression-00fc3b51f2e1488eb76fa5aaecb04ab5/中文 用户路径/`。

## 交付

- 单端 EXE：`dist/PlanePet-Release-Single-1.0.3/PlanePet.exe`
- 单端 ZIP：`dist/PlanePet-Release-Single-1.0.3.zip`
- 单机双端公网 ZIP：`dist/PlanePet-Release-DualLocal-1.0.3.zip`

SHA-256：

| 产物 | SHA-256 |
| --- | --- |
| 单端 EXE | `088eb68981154578b037890580f503dda05be80f3872aa2cd8df8da5d56f8376` |
| 单端 ZIP | `3f65cae19d3e5e7919327e0476bafbbc0e358641f3f7ac9ca6573f812f80f168` |
| 双端 ZIP | `ee80a7668d1d3f60cceb4359ee678f1315be6518ecd78eb9e69ced9dc96d0528` |

## 已知边界

- 备份可能少于最新一局；没有有效备份时不猜造历史，也不自动清空损坏原档。保留备份不是对所有合法格式内的任意位翻转提供检测保证。
- 已有旧日志为保护数据原样保留，可能超过新日志大小限额；最多使用 16 个旧档槽，槽满时提示本地写盘失败而不是覆盖旧档。滚动日志是有限诊断样本，不是永久全量统计档案。关闭前、断电或极端磁盘阻塞可能丢失尚未落盘的可选统计。
- 当前发布包仍未做代码签名；本轮不涉及购买证书、上传正式清单、服务器扩容或服务器隐私删除流程。此前单独列出的上线运营项并未因此视为完成。
- 本轮未改服务器/云端，不清零真实统计，不改 FingerKnight；公网对战通道配置沿用，但没有产生新的公网对战测试数据。测试覆盖不等于承诺在所有设备与网络上绝无错误。
