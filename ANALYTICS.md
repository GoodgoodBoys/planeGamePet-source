# Plane Pet 匿名测试统计后台

## 访问

- 公网地址：`https://8.166.124.212/admin`
- 用户名：`plane-pet`
- 密码只保存在 ECS：`/etc/plane-pet/gateway-admin.password`
- 页面每 60 秒刷新，必须通过有效 TLS 和 HTTP Basic 鉴权访问。

## 采集范围

只有用户明确同意后，公网客户端才会发送：

- 程序启动、正常退出和每 60 秒一次的会话心跳；
- 匹配开始、停止和成功；
- 邀请发出、接受、拒绝、取消和超时；
- 对局开始、结束、用时、胜负及结束原因；
- 四种固定快捷表情的发送类型与好友端接收次数；
- 桌宠显示、隐藏和勿扰状态变化；1.0.4 增加邀请前勿扰拦截、等待中勿扰终止、已授权运行期间的勿扰时长；
- 升级检查、发现版本、用户选择、下载/安装结果、失败及回滚。

不采集聊天文字、匹配码、姓名、键鼠轨迹、屏幕内容、窗口标题或其他应用信息。匿名安装标识由
随机隧道凭据的 SHA-256 哈希截取生成，不能直接还原为凭据。网关只持久化用于防滥用限流的
不可逆来源分组值，不保存原始来源 IP；公网隧道和健康检查关闭 Nginx 访问日志。

## 数据与指标

- SQLite：`/var/lib/plane-pet/telemetry.db`
- 原始事件默认保留 90 天；后台每分钟清理，管理页生成前也排除过期事件。
- “启动会话”按实际收到事件的安装标识与会话标识组合去重；中途同意统计也计入，不要求收到启动事件。
- 新客户端只累计启用统计的时间段；旧客户端按实际收到事件的首尾时间差估算，不倒推授权前的使用时间。断电或漏报可能造成少量误差。
- 邀请和对局以服务端下发的匿名 `invite_id` / `round_id` 去重，双方上报不会重复算一局。
- 接受/拒绝/超时只计算保留期内具有发出记录的同一批邀请。孤立结果单列，不纳入比例，也不当作拒绝。只看到同意统计的样本，不能视为全体用户数据。
- 对局完成率仅计算有开局记录的对局（包括已接受邀请的倒数）。3.0.1 起，“完成”只包括生命耗尽或达到时限的正常结算；异常中止、退出／掉线结束分别展示，不纳入完成率的分子或平均时长。
- 平均时长采用正常完成对局的实际战斗用时，包含服务器确认等待；上限 182 秒，避免将三分钟比赛加少量确认延迟误排除。仍拒绝超出此界限的异常时长。
- 旧版（0.x–2.x）缺少结束原因的记录保留原完成口径，并单列数量注明不确定性；当前版本缺少原因的结束记录列为待确认。迟到的权威结果可更正此前的客户端断线分类。只改变读取计算，不改 SQLite 结构、不改写历史原始事件。
- “当前活跃”指最近 120 秒内上报过事件或心跳、且同意统计的匿名安装。
- “快捷表情”独立展示发送总数、好友端收到总数、使用人数和四类表情分布。

## 运维

1.0.3 增加名额剩余、连接数和统计写入失败/队列丢弃监测。当前默认限制为 128 条连接、累计 1000 个安装凭据、每 IP 每小时 64 次新登记；维护不能随意删除终身绑定。

`/healthz` 为网关存活检查；仅本机可用的 `/readyz` 检查对战服务响应，不宣称磁盘与全部业务都健康。认证后的本机 `/admin/status` 提供容量与统计队列 JSON，现有 Nginx 默认不公开这个新路径。

查看状态：

```bash
systemctl status plane-pet-gateway.service
journalctl -u plane-pet-gateway.service --since today
```

轮换管理员密码：

```bash
openssl rand -out /etc/plane-pet/gateway-admin.password -hex 24
chown root:plane-pet /etc/plane-pet/gateway-admin.password
chmod 0640 /etc/plane-pet/gateway-admin.password
systemctl restart plane-pet-gateway.service
```

备份数据库时使用 SQLite 在线备份命令或先停止网关，避免只复制主数据库而遗漏 WAL。
服务器配置和旧二进制的部署前备份保存在 `/var/backups/plane-pet` 或原文件旁的
`.pre-analytics-20260902` 文件中。
