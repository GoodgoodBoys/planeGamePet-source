# 正式版后续运营与 2000 连接方案

2026-09-09。本文对应发布检查第 5、6、7 项，是实施方案，不代表这些后台能力已经上线。未购买云资源、扩大访问权限、修改 FingerKnight 或提高现网连接上限。

## 已确定的边界

- 开发者 Ding，反馈及隐私联系邮箱 `planepet_public@163.com`。
- 正式版本从 1.0.0 开始；历史测试者手动替换一次，保留本地存档，后续使用独立正式更新通道。
- Windows 发布者签名暂缓；更新清单签名保留。独立双机/长期验收和中国公开发行适用要求按开发者决定另行处理。
- “2000 条线路”暂按 2000 个同时在线客户端理解，最多 1000 场双人对战。若指 2000 场对战，则应按 4000 客户端重新测算。

## 5. 隐私、反馈与服务器数据处理

### 当前事实与目标

程序已有可选统计开关、本地清除和反馈邮箱。必要联网凭据会经 TLS 传输，服务器保存摘要而非原始令牌；可选事件按持久安装摘要关联，不应称为绝对不可关联的匿名数据。本地清除不等于删除服务器数据。现有原始统计保留上限为 90 天；本方案不会悄悄修改该值。

应提供可验证、可追踪且尽量少收集信息的申请流程。《个人信息保护法》涉及告知、撤回同意、删除等要求；具体适用性、例外及留存期限需结合实际运营核定，下面是工程设计而非合规结论。[法律原文](https://www.samr.gov.cn/wljys/gzzd/art/2023/art_3ef1e889c1e644d4b65b5f5c7f432386.html)

### 用户流程

1. “关于 → 隐私说明”说明必要联网与可选统计的区别；提供“申请处理我的服务器数据”入口，默认不自动发邮件。
2. 用户选择：仅删除可选统计；解除绑定并撤销本机联网身份；或咨询/导出自己的记录。明确每种操作的影响，不能用一个“清零”按钮混合处理。
3. 当前安装通过既有 TLS 鉴权向 Plane Pet 后台获取一次性申请码：随机至少 128 位、15 分钟有效、一次性使用、每安装与来源限流。服务器仅保存码的摘要和处理范围。
4. 用户向反馈邮箱提供申请码及诉求，不发送原始令牌、匹配码、完整诊断日志、身份证或朋友的资料。
5. 后台核验码与当前安装，处理前再次确认范围。安装凭据丢失时，不凭昵称或六位匹配码推断身份，也不承诺能找回旧记录。
6. 回执只说明已处理的类型、时间和备份清理安排，不带朋友标识或后台访问链接。建议运营目标为两个工作日内确认收到、核验后十五日内完成；这是待确认的服务目标，不是对法定期限的判断。

### 后台实现及兼容

- 为每个申请生成事务 ID；状态为待验证、待确认、处理中、完成、拒绝/过期。审计只保留必要的事务摘要、时间、范围与结果，不复制已申请删除的原始数据。
- 统计删除先设“禁止补写”标记，再丢弃队列内该安装事件，事务删除相应原始行并重建受影响汇总；验证后才完成回执。网络重试不能把已删除事件写回来。
- 删除统计不解绑；解绑/撤销身份则先拒绝后续鉴权及动作，再正常取消邀请、结束会话、解除对应关系。不能删除朋友自己的全部战绩或其他安装记录。
- 同一对局由双方上报：申请人原始事件可以删除；保留对方合法记录时避免继续保留指向申请人的关联字段。统计需重新计算，不得简单把总数减一。
- 数据库用增量迁移；旧客户端没有申请入口仍可通过邮箱处理。新增事件要有独立 schema、release_epoch 和白名单，旧记录不能重写成新事件。
- 从备份恢复前先应用删除清单，阻止已删除数据复活。备份按约定期限自然轮换，而不是为一个申请擦除整台共享服务器。

### 留存与实施验收

建议继续保留原始可选统计最长 90 天，普通排错日志建议不超过 30 天，加密备份建议最多 28 天；安全日志的适用法定留存要求需另行核定，不可一刀切承诺 30 天。永久绑定属于持续提供功能所需数据，明确解绑/注销后的清理规则。

实施前需确认：最终处理范围、留存期限、邮箱处理责任人。先做合成数据测试：重复申请、伪造/过期码、撤回、队列迟到事件、朋友数据保护、备份恢复、防重复扣减；再同步首次提示、帮助、关于、随包隐私文本。没有实现的功能不能先写成“已支持”。

## 6. 监控、加密异机备份与故障处理

### 证书已落地的部分

Plane Pet 专用任务每小时检查一次、无结束日期，仅续期 IP `8.166.124.212` 的证书，不运行共享目录里的部署 hook。核对实际提供的证书、信任链、IP SAN 与剩余有效期；必要时先检查 Nginx，再平滑重载。已有演练及实际签发/加载验证。该 IP 证书只能签发 160 小时有效期，长期使用依赖持续自动续期，不能把证书一次改为几年。[Let's Encrypt 说明](https://letsencrypt.org/2026/01/15/6day-and-ip-general-availability.html)

本机状态文件和失败日志已具备，外部告警接收通道尚未配置。服务器续费、网络、验证入口和 CA 可用性仍是持续运行的条件。

### 监控设计

| 检查 | 建议频率与阈值 | 处理 |
| --- | --- | --- |
| 公网 health + readyz、有限频率的真实 TLS 探针 | 每分钟；连续 3 次失败 | 告警，避免单次抖动刷屏 |
| 证书签发/部署结果、实际到期时间 | 每小时；少于 48 小时预警、24 小时紧急 | 明确区分签发失败与 Nginx 未加载 |
| 连接数、拒绝数、事件循环延迟、对战 tick 耗时 | 每分钟；容量 80% 预警、90% 紧急 | 限制新连接，不踢正常对局 |
| CPU、可用内存、磁盘、进程异常重启 | 每分钟；持续高负载、磁盘剩余低于 20%/10% | 记录原因与容量趋势 |
| 统计队列、丢弃数、写入失败 | 持续计数、按分钟汇总 | 统计故障不能拖慢对战 |
| 备份年龄、上传结果、恢复演练时间 | 每小时 | 超过计划间隔两倍告警 |

监控端尽量放在另一故障域；告警内容仅含服务名、时间、错误类型和概览指标。不要把安装凭据、后台密码、完整 IP/个人事件发到邮件。告警需去重、恢复通知、升级机制及人工确认，不能只靠“服务 active”。

### 备份设计

1. 使用新的私有 OSS 备份位置，和公开下载前缀彻底分离；先确认成本与权限，再创建。绝不更改现有 Bucket 的全局 ACL。
2. 仅备份 Plane Pet 的绑定、联网凭据摘要库、统计数据库、必要配置和版本清单。SQLite 使用在线 backup API，不直接复制活跃 WAL 数据库；多文件需记录统一检查点，必要时短暂暂停本项目写入，不能宣称每个文件分别一致就等于整体一致。
3. 先在本地用离线保管私钥对应的公钥加密，再上传；服务器只持有加密公钥及最小写权限，恢复私钥不得放入源码、EXE 或公开 OSS。可再叠加 OSS 服务端加密，但不能以其替代应用侧加密。
4. 建议每小时数据备份、每日完整备份，保留最近 24 个小时点、7 个日点和4个周点，最长 28 天。删除清单的保留期至少覆盖可恢复备份窗口。生命周期、覆盖策略与费用需批准后实施。
5. 上传后比对密文大小和摘要、记录备份元数据；失败留告警，不能仅靠上传命令退出码。
6. 每月在独立目录/服务器恢复演练：解密、完整性、数据库迁移、绑定一致性、删除清单重放、启动与模拟对局。严禁用线上原路径做恢复试验。

建议目标 RPO≤1 小时、RTO≤2 小时，仅在演练证明后作为承诺。数据量、网络和运维响应不满足时应调整。

### 事件处理和回滚

先确认影响范围 → 停止本项目新发布 → 保全必要脱敏证据 → 使用上一份代码回退或执行经过审核的恢复 → 校验统计、绑定及朋友状态 → 通知恢复。数据库优先前向兼容，代码回退不能直接恢复旧整库抹掉新用户数据。FingerKnight、旧设备服务、共享安全组和整机磁盘都不是 Plane Pet 的回滚目标。

待确认：告警邮箱/渠道、备份位置及预算、恢复私钥保管人。实施验收包括证书加载失败、服务失联、磁盘不足、上传失败、过期备份、实际解密恢复；这些故障注入只在隔离环境进行。

## 7. 2000 个同时在线客户端

### 现网核实与结论

2026-09-09 ECS 控制台：`ecs.e-c1m1.large`，2 vCPU、2 GiB，公网 100 Mbps 峰值，按使用流量计费。系统可见内存约 1.57 GiB，当前还共用 FingerKnight。按流量计费的峰值不是持续带宽承诺。[阿里云说明](https://help.aliyun.com/zh/ecs/user-guide/network-bandwidth)

现网网关最多 128 条同时连接、1000 份累计安装登记；同一公网 IP 最多 64 条 Nginx 连接。Nginx 为两个 worker、每个 `worker_connections=768`，代理连接还包含上游连接，所以不能简单认为有 1536 个玩家名额，理论上约 768 条代理隧道还要扣除其他流量。[Nginx 定义](https://nginx.org/en/docs/ngx_core_module.html#worker_connections)

**当前不能支持或承诺 2000 客户端。** 仅改 `128` 为 `2000` 不解决 Nginx、CPU、带宽、登记上限或办公室共享公网 IP 的限制；更不能靠删永久绑定腾位置。现有双隧道 RTT 测试和 128 路本地回声压测都不是 2000 路全链路验收。

### 流量估算：闲置和对战不能混算

当前代码状态广播为 15 Hz（菜单）/30 Hz（对战），对战仿真 60 Hz。完整战斗状态最多 191 字节，加应用鉴权 8 字节。仅按满状态包估算：

`2000 × 30 × (191 + 8) × 8 ≈ 95.52 Mbps`

这还没算 WebSocket/TLS/TCP/IP、心跳、重传、输入转发、命中事件，不是所有人的最低需求，也不是实测平均值。协议还可每次转发最多 48 条输入（181+8 字节）和最多 6 次命中（153+8 字节）；三类都取上限时应用层本身约 263.52 Mbps。这是保守边界，正常平均往往低于它，但 100 Mbps 不适合作为 1000 场同时对战的容量保证。

菜单也有状态消息，不能把 2000 个在线桌宠当成几乎零带宽。后续应先实测菜单负载，设计“状态变化推送 + 低频保活”；桌宠飞行和云由本地计算，不应靠服务器逐帧同步。不得为省带宽擅自降低对战 tick 或改动命中判定。

100 Mbps 持续出网一小时约 45 GB（十进制），费用取决于实际出网而非 EXE 下载量；1000 用户仅偶尔点一局与长时间全员对战的成本差异很大。程序分发继续走 OSS，不能由 ECS 反复承担 EXE 下载。

### 推荐实施顺序

1. **明确容量口径**：同时在线 2000、对战占比 10%/50%/100%、单 IP 集中人数、可接受拥塞提示、每天活跃时长与预算。2000 条排队等待不等于 2000 条可用连接。
2. **先优化和加观测**：建立绑定/房间索引，避免随总用户增长的线性查找；测量单 tick、Python 事件循环、队列和 SQLite 写入。对菜单改为状态推送/低频保活，更新协议时保留旧客户端兼容策略。所有队列有上限、迟到包可丢弃、动作不可重放。
3. **使用独立 Plane Pet 压测环境**，不在 FingerKnight 生产机做大流量测试。可先比较 4 核 8 GiB 与 8 核 16 GiB 候选；2000 全员对战优先评估后者与至少 300 Mbps 级带宽预算。规格只是压测起点，不是保证或购买建议。
4. **扩大保护性配置**：专用 Nginx worker_connections/文件句柄、网关限额、累计登记额度与注册速率分别规划。办公网络需“每安装限额 + IP 弹性限额 + 滥用阻断”，不能直接取消所有 IP 限制。新用户超额得到明确提示，既有连接不被随机踢掉。
5. **必要时按绑定分片**：当前状态服务器和 Python 网关不是直接多开进程就能共享房间；先保证同一绑定固定路由到同一游戏分片、永久库唯一写入及明确迁移协议，再部署两个节点。跨节点故障本局明确中止即可，不为此强加无缝重连需求。
6. **小流量逐级验收**：100→250→500→1000→1500→2000，另测约 25% 余量。分别模拟纯菜单、10%/50%/100% 同时对战、同 IP 批量连接、注册突发、邀请风暴、断网重连和统计拒绝/同意。生成器使用独立机器，完整走公网 TLS、Nginx、网关及真实游戏协议。
7. **至少两小时稳定运行**：验收 tick p99 小于 16.7 ms、网关新增处理耗时 p95 小于 10 ms、没有同步超时/队列持续增长/统计写入积压，CPU 常态保留至少 30% 余量、内存不持续上涨。绝对公网 RTT 还依赖距离和运营商，单独记录，不把它全归为服务器耗时。
8. **灰度上线**：按达标容量逐步放量，监控和回滚就绪后再提高上限。CPU、内存、延迟或错误率越界即停止测试/扩量。扩容和付费操作另行获得批准。

优先建议是先确保 2000 人在线且少量同时游戏，再决定是否为最坏的 1000 场同时对战预付容量。若必须承诺全员同时对战，则以最坏负载验收，而不是以平均体验代替。
