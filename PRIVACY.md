# PlanePet 隐私与数据说明

开发者署名：Ding
说明更新日期：2026 年 9 月 8 日
反馈邮箱：planepet_public@163.com

本说明适用于 PlanePet 的 PC 对 PC 测试版本。阅读本页面不会开启使用统计，也不会上传阅读记录。

## 一、提供联网功能所需的数据

为完成好友绑定、在线状态显示、邀请、表情和对战，程序会通过服务器交换匹配码、在线与勿扰状态、版本信息、游戏操作及对战状态等相应数据。服务器在提供连接和防滥用服务时，也会处理来源 IP 等必要网络信息。

程序使用随机生成的安装凭据、设备和绑定标识来识别设备、恢复绑定。本机联网凭据由 Windows DPAPI 保护；凭据通过加密连接传输用于鉴权，服务器持久保存其摘要，而不是原始令牌。服务器另有登记时间和防滥用来源分组。

六位匹配码仅用于当次查找，不是长期密码。同码先到的两个客户端会配对，请事先与朋友约定不易冲突的匹配码。

公网对战与统计由阿里云 ECS 上的配套服务提供，软件更新文件通过阿里云 OSS 分发。访问这些服务涉及必要的网络通信，不等同于你同意了可选使用统计。

## 二、可选的使用统计

只有首次运行时明确同意，或之后主动开启统计，程序才会上报使用统计，用于了解使用情况和改进体验。可在右键菜单“设置与隐私 → 匿名使用统计”中随时关闭。拒绝或关闭可选统计，不影响正常绑定和对战；未授权期间的活动不会事后补传。

统计范围包括启动和退出、启用统计期间的使用时长、匹配与好友在线变化、邀请响应、显示与隐藏、勿扰使用情况、四种固定表情类型、对局结果与结束原因，以及软件更新的检查、选择、下载、安装和回滚结果。

使用统计不包含聊天文字、匹配码、姓名、键鼠轨迹、屏幕内容、窗口标题或其他应用的信息。对战所需的飞机移动数据属于上一节的联网功能数据，不是桌面操作监控。

统计使用随机标识区分安装实例，可以关联同一次安装的使用情况。因此，菜单中“匿名使用统计”不代表完全无法关联的匿名化数据。统计结果仅代表同意且成功上报的样本，断电或网络漏报可能造成误差。

## 三、数据保存与传输

联网传输使用加密连接。服务端统计事件默认按 90 天保留期限清理；关闭统计后，不再新增未授权的使用记录，但此前已上传的记录不会因此立即删除。

联网身份、绑定和必要运维记录不属于上述可选统计事件，不会随统计开关或统计事件到期自动一并删除。当前测试版尚未提供完整的服务器资料自助删除功能；联网身份、运维日志及备份的统一删除与到期规则仍需在正式发布前完善，不能将“90 天”理解为所有数据的保存期限。

## 四、本地保存、导出与清除

本机保存绑定信息、设置、累计战绩和最近十局详情；启用统计时另有本地事件记录。

“设置与隐私 → 清除本地战绩与统计…”只清除本机战绩和实验记录，不会删除绑定或服务器已有记录。解除绑定需要选择同一菜单中的“解除绑定…”并确认。

“导出诊断信息”仅导出程序版本、网络状态与错误码、是否绑定、保存是否成功和统计开关等状态，不包含密钥、匹配码、身份 ID、用户名、完整路径或原始日志。诊断文件不会自动上传，请先自行查看内容。

## 五、联系与数据请求

如需反馈使用问题或提出数据相关请求，可发送邮件至 planepet_public@163.com。请勿在邮件中发送密码、联网凭据、绑定文件或其他不必要的敏感资料。

正式公开发布前，将继续完善数据请求的身份验证与处理流程，以及必要的保留和备份处理规则。当前不能承诺本地清除等同于删除服务器上的全部数据。
