# PlanePet 3.0.12 关于页紧凑排版

日期：2026-09-09。

仅调整关于、隐私说明、第三方许可页面。正文与链接逻辑字号 14 → 13，标题 22 → 20；继续按 DPI 创建原生字体，不缩放文字位图。

关于页目标客户区从 440 × 330 改为 380 × 264 DIP，图标改为 40 DIP。图标和版本组成标题区；简介、开发者/邮箱两列、底部链接分组排列，增加细分隔线，统一左侧对齐。文档页保留 440 DIP 阅读宽度，高度 580 → 560 DIP，压缩标题区并保留滚动阅读、选择复制和固定返回链接。

未修改桌宠、游戏、帮助、历史战绩的字体或布局；未修改服务器、统计行为、隐私和许可正文、邮箱、用户存档，未上传 OSS 或 GitHub。

## 验证

- `build.ps1 -BuildPublic` 完整编译成功，生成独立 3.0.12 单端、双端包，不覆盖旧版本目录。
- 实际桌面隔离回归通过，包含 96 / 120 / 144 / 192 DPI 的字体、联系信息列间距、邮箱完整显示、链接边距/文字不截断、文档对齐与返回操作。
- 首轮新增的高 DPI 高度断言因注入 WM_DPICHANGED 不改变系统标题栏 DPI 而误报；改为校验目标外框，并独立按实际客户区校验控件间距。未删除检查，复跑通过。
- 已查看原生窗口截图：100%、150%、200% 关于页、隐私页和第三方许可页，无文字遮挡或截断。
- 单端、双端发行包静态检查，内嵌组件与文档哈希比对，帮助文案一致性和 `git diff --check` 通过。

最终回归报告：`dist/release-regression-99fa5664025549248a6ee705ce6fe6ea/中文 用户路径/检查结果.txt`。

单端 EXE：`dist/PlanePet-Public-Single-3.0.12/PlanePet.exe`

SHA-256：`06605600f4c126c4e86f8df29eca8d32766cf3980516df13999b78b7bb953d89`

完整包：`dist/PlanePet-Public-Single-3.0.12.zip`

仍为未签名候选版本；本轮不包含公网双机实战或其他上线待办验收。体验新版前应先从旧版菜单退出，避免单实例机制唤回旧程序。
