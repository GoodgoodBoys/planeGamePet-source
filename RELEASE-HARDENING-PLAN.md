# 正式发布完善计划（1.0.3 候选版）

本轮先修复发布审查 1～4，再落实 5～11 中能安全完成的代码、工具和说明。保持 1.x 协议/存档兼容，不修改 ESP32 场景或同机 FingerKnight，不自动发布 OSS stable 清单或推送 GitHub。

用户追加确认的菜单精简已实现：常态一级七项，统计/数据/解绑收进“设置与隐私”，帮助/升级/诊断收进“帮助与更新”；桌宠与托盘共用构建及命令路径，最多两层。新增 28 项菜单回归，总计 86 项窗口检查；原紧凑尺寸与公网直连保持不变。帮助和随包说明已同步，详情见 `FIX-VERIFICATION-1.0.3.md`。

后续邀请卡片留白调整已完成：接受/拒绝/取消按钮与卡片至少间隔 10 像素，整体桌宠仍为 280×150。新增 44 项布局/文字/四档 DPI 检查，窗口回归现合计 130 项；真实客户端按钮边缘与留白点击回归通过，单/双端包已重新编译并核对内嵌组件。

| 项 | 实施内容 | 验收 / 外部依赖 |
|---|---|---|
| 1 中文路径 | Unicode 参数解析、严格参数边界、Unicode 存档回归 | 中文/空格/非 ASCII 路径与名称 |
| 2 升级恢复 | 启动失败专用状态、客户端就绪握手、保留最后稳定 EXE | 状态竞争、安装成功、启动失败和中断恢复 |
| 3 统计 | 邀请/接受同一 ID 集合计算，展示孤立结果，保持数据库兼容 | 漏报、乱序、重复及过期窗口测试 |
| 4 找回桌宠 | TaskbarCreated 重建、二次启动激活、快捷键冲突提示 | 独立窗口注入消息，不重启用户 Explorer |
| 5 签名 | 可选构建签名、正式发布签名检查门禁，签名先于嵌入/摘要 | 需发布者提供有效证书与时间戳服务；不购买或伪造证书 |
| 6 网络 | 游戏直连、错误分类、退避重试、脱敏网络状态 | 不继承可能绕远的系统 HTTP 代理；保留入口，443 迁移需确认 |
| 7 容量 | 容量/拒绝原因可观测化，保留限流和终身绑定 | 不直接放大生产限额；需首批规模和压力验证 |
| 8 性能/DPI | 减少重复轮询、保留模态消息泵；分屏缩放回归计划 | 有界性能测试；真实多屏/高 DPI 需环境验收 |
| 9 运维/发布 | 项目限定健康检查、备份恢复步骤、一致发布与密钥轮换 | 告警接收方、异机备份位置和生产部署需确认/验收 |
| 10 文档/支持 | 帮助/隐私/统计口径同步，脱敏诊断导出、许可清单 | 不虚构运营方或素材授权；联系方式待用户提供 |
| 11 验收 | 自动回归、隔离公网候选包、人工矩阵 | 实体双机/代理/安全软件/跨盘未测的不标通过 |

顺序：A 修复验证 1～4 → B 网络/容量/帮助/诊断 → C 签名与运维工具 → D 构建回归 → E 外部阻塞及人工验收。候选包与已发布 1.0.2 分开，测试使用隔离数据目录。

## 5～11 的后续实施与验收

### 5. 发布者签名（代码接入完成，证书待提供；复杂度中）

- 已实现：按证书指纹调用 SignTool，SHA-256 文件摘要、RFC3161 时间戳、签后验证；组件先签名再嵌入启动器，最后签启动器。`-RequireSignature` 在未提供证书时阻断正式构建。
- 仍需：发布者有效代码签名证书、批准的时间戳地址、Windows SDK SignTool 路径。硬件令牌/云签名应单独接入供应商支持流程，不导出私钥绕过其保护。
- 验收：成品与嵌入组件均显示正确发布者；时间戳有效；SHA/更新清单从签名后的最终文件生成。在干净 Windows 和实际安全软件环境运行，签名不等于保证无 SmartScreen 提示。
- 注意：Windows 发布者签名与现有 OSS 更新清单签名是两套机制，均保留；不能用前者替代更新包 SHA 和清单验签。

### 6. 网络可用性（客户端基础完成，入口迁移待确认；复杂度中）

- 已实现：错误分类、退避重连、限流等待、脱敏连接诊断及手动导出。2026-09-06 用户反馈后撤回游戏隧道的系统自动代理：真实同机对照旧直连 46ms、新代理路径 734/875ms；仅恢复隧道直连后两端中位均 47ms。更新下载保留系统代理，不改全局设置或服务器配置。
- 仍需：受限网络/认证代理/PAC/禁用非标准端口环境验收。游戏直连恢复原有行为；不得默默切入高延迟代理，也不承诺绕过组织限制。未来若需游戏代理，应提供显式选择并测量实际 RTT。
- 443 迁移：先确定 PlanePet 专用域名和证书，只新增项目限定入口；验证 WebSocket Upgrade 与路由，不覆盖共享 Nginx 配置。保留旧入口给 1.0.2，候选包切换到新入口后灰度验证，再决定旧入口退出时间。
- 验收：普通家用网、手机热点、代理及断网恢复均有明确结果；TLS 错误拒绝连接；服务器重定向不得携带安装凭据转发；重连不会造成请求风暴。

### 7. 容量与防滥用（可观测性完成，规模验证待执行；复杂度中）

- 已实现：连接/累计凭据/登记限额、80% 容量预警、明确拒绝原因与 Retry-After；已有安装在新登记名额耗尽后仍可使用。没有删除永久绑定或直接扩大生产限制。
- 已验证：本地 128 条隧道、超额拒绝、3 波共 48 次重连、2944 次回环通过，结束计数归零；不是包含 Nginx/TLS/完整对战的 ECS 承载证明。
- 仍需用户提供：首批测试人数、预计同时在线数和是否允许每台电脑双端测试。128 是连接数，不是 128 对好友；1000 是累计登记容量，不是每日配额。
- 压测只在隔离服务、端口和合成数据中进行：覆盖正常并发、达到限制、每安装连接超限、短时重连、SQLite 忙及管理页面并发。测试时设置资源上限，不能让共享 ECS 的另一项目承担压力。
- 验收：容量达到预算时延迟/CPU/内存有记录；超额请求有界拒绝，已有连接不因新登记耗尽而被踢；统计队列有上限，数据库忙不阻塞游戏事件循环。扩容需依据测量值，不把删除身份当扩容。

### 8. 待机性能与 DPI（代码及四档自动回归完成，真实混合屏待测；复杂度高）

- 已实现：消除重复 1ms 轮询，统一消息定时泵；可见待机约 30fps，游戏保留约 60fps 调度，隐藏不绘制。帮助/历史/诊断窗口处理 DPI 变更。
- 已测：本机相同未绑定/显示配对卡场景，各采样 12 秒，单核 CPU 从 36.30% 降到 8.07%；隐藏从 0.26% 降到采样精度下 0.00%。不是整机百分比，也不是长期稳定性结论。
- 较早 DPI 放大候选版复测：同样各 12 秒，48.53%→10.14%；隐藏 0.26%→采样精度下 0.00%。这是历史短测，不是恢复紧凑尺寸后的性能保证。
- 2026-09-06 按用户要求保留原物理尺寸：桌宠 280×150、游戏内容区 240×372、更新卡 360×244，字体不额外放大。保留 DPI 感知、原生清晰字体、GDI/GDI+ 一致坐标、鼠标命中与工作区边界修复；仅原生标题栏按真实 HWND DPI 取尺寸。帮助/历史维持先前策略。58 项回归包含四档 DPI 消息、固定尺寸、更新关闭、表情命中、完整地图和结算返回；真实公网双客户端也验证游戏区均 240×372。
- 验收：100/125/150/200% 与混合 DPI 双屏、负坐标屏幕、拔出显示器；飞机/血条/按钮无裁切，输入坐标准确，窗口返回可见工作区。再做至少 2 小时待机/隐藏/追击和多轮对战的 CPU、内存、GDI/USER 句柄趋势测试。
- 发布门槛：代码和四档自动化通过，不等同于真实混合 DPI 双屏拖动和拔屏已验收；极小工作区、高于 200% 的缩放也仍需环境测试。

### 9. 运维、备份与发布救援（部署及恢复演练完成，持续监控未启用；复杂度中高）

- 已实现：只读后端就绪探针、受认证的状态指标、白名单备份和 SHA/SQLite 完整性验证。备份工具经过 Windows/Linux 测试，并在 ECS 暂存副本验证旧新网关读取现有统计兼容。
- 已部署：2026-09-06 在无活动 PlanePet 隧道时，仅替换并短暂重启 PlanePet 两项服务。备份、代码哈希、就绪/公网 TLS/后台鉴权均通过，1878 条历史记录与绑定/凭据保留。Nginx、服务配置、FingerKnight 程序摘要及相关服务 PID/状态前后相同。
- 待配置：告警接收方、专用加密异机备份位置、保留周期。定时检查服务/就绪、证书到期、容量、磁盘、统计丢弃和备份年龄；本轮没有创建无人接收的告警或把敏感凭据放到公共 OSS。
- 恢复演练已执行：备份副本在隔离目录/端口启动，3 对绑定、6 个端点恢复连接，14 份网关凭据加载，后台可读，备份与生产数据未修改。此次部署备份在暂停两项 PlanePet 服务后创建；日常活跃备份仍不等同于跨三份文件的全局事务快照。
- 升级救援：1.0.3 升级器保留 `.last-good`；由 1.0.2 发起的首次升级仍执行旧升级器，不能倒推承诺旧版也有新备份策略。首轮过渡升级需独立验收。
- 密钥轮换仍需兼容协议设计：先通过可信版本分发双钥信任，再切发布钥、最后撤旧钥；不能直接更换固定公钥导致旧客户端永久失联。当前未实现双钥/撤销协议，泄露救援需可信外部分发渠道。

### 10. 帮助、隐私和支持（程序说明已补充，运营信息待提供；复杂度中）

- 已实现：菜单帮助同步最新操作、统计同意涵盖更新事件、诊断导出脱敏；单/双端包包含隐私说明、组件说明及实际工具链许可文本，EXE 也内嵌说明用于升级后保留。
- 待用户确认：发布者名称、公开反馈渠道、源码许可选择、参考素材/最终资源的发布授权。不得虚构这些信息。
- 服务端数据删除还需完整流程：申请者如何验证归属、解绑与删除顺序、防止删除他人绑定、保留期与备份失效策略。当前本地清除不能表示远端已删除；关闭可选统计不能删除联网必需身份。
- 验收：首次同意、关闭统计、异常结束、升级后旧设置均符合说明；所有帮助内容与实际菜单一致。测试发起者能凭脱敏诊断处理故障，不索取令牌、私钥或原始凭据文件。

### 11. 最终验收和发布（自动化已通过，真实环境仍待测；复杂度中高）

- 已执行的自动化及证据见 `FIX-VERIFICATION-1.0.3.md`；独立环境矩阵见 `RELEASE-ACCEPTANCE.md`。
- 用户退出旧实例后，已运行从真实 1.0.2 EXE 提取的原升级器，替换隔离目录中的旧程序，启动最终 1.0.3 启动器/客户端/隧道并通过就绪及公网 TLS 检查；合成身份、设置、战绩保持不变，原用户档案哈希未改变。未改 OSS stable，因此未把此项冒充为已完成线上“检测→下载→安装”的全流程推广验收。
- 然后实体双机/不同网络测试、中文账号、跨卷安装、DPI、安全软件、代理和长时间运行。故障注入使用测试副本，不在用户真实数据上制造磁盘满或断电。
- 只在上述必要门槛满足后，生成最终签名构建→最终摘要→更新清单签名→候选试发→授权生产部署与 OSS stable 推广→分别更新源码和发行仓库。未授权前不推送、不覆盖已发布 1.0.2。

## 当前停点

代码与候选包已经包含本轮修复，服务端修复已经上线；真实旧升级器→新启动器、四档 DPI、128 条本地隧道容量与独立备份恢复演练已完成。仍需真实多屏/双机/代理/长时间测试，以及签名/域名/联系方式/首批规模/告警和异机备份位置等运营方选择；未推送 GitHub 或更新 OSS stable。
