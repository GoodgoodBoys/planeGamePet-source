# 1.0.12：鼠标悬停操作栏与一键开局

2026-09-07。本次落实已确认的静态 C 拳头方案，只修改桌宠操作入口、相应资源、帮助说明和测试。不改变玩法、联网协议、统计事件格式或身份存档格式。未操作 ECS、OSS、GitHub 或 FingerKnight，未改真实用户绑定、战绩或统计数据。

## 使用方式与尺寸

- 鼠标进入整个桌宠窗口范围时显示操作栏，包括飞机之间的透明区域；透明区域仍可穿透点击，不增加全窗口鼠标拦截。
- 默认显示静态 C 拳头 +“开一局”，右侧是同高窄按钮 ◀。点击“开一局”使用原有邀请流程；右键邀请入口保留。
- 点击 ◀，四个原有表情从右向左滑出，覆盖“开一局”；箭头变为 ▶，再次点击收起。滑动用时 180 毫秒，可在过程中反向收起。
- 鼠标移出 400 毫秒后隐藏，避免边缘抖动。完全隐藏后再次移入默认显示“开一局”。
- 桌宠窗口保持 280 × 150 物理像素；四个表情按钮保持 35 × 33，图标显示区保持 23 × 24，位置、间距和图标素材不变。
- “开一局”覆盖原四按钮的 149 × 33 区域，右侧箭头 22 × 33；未增大原有桌宠或游戏窗口。
- 已选 C 拳头使用静态原图，不播放攻击动画；原四个表情及勾引动画的行为不变。

## 状态隔离与代码复核

- 新按钮在鼠标松开且仍处于原按钮时才生效。按下后移出、拖出再移回、捕获丢失、隐藏和 DPI 改变会取消点击，避免误邀。
- 展开 / 收起过程中不接受“开一局”和表情点击，避免动画中的目标错位；箭头仍可反向操作。
- 离线或大版本不兼容时开局按钮禁用。匹配、解除绑定、可靠控制消息待确认、邀请等待、游戏、隐藏及重叠的连接错误提示期间不显示普通操作栏。
- 自己开启勿扰仍可邀请；对方勿扰继续显示原有可取消的 3 秒提示。普通操作栏隐藏不会隐藏邀请接受 / 拒绝 / 取消按钮或勿扰提示。
- 使用既有 UI 定时器查询鼠标位置，没有新增鼠标钩子、后台线程、网络轮询或统计事件。普通在线快照不会反复复位操作栏。
- C 图标缓存复用；绘图裁剪和 GDI 对象正确恢复 / 释放。100 次重复绘制未增加 GDI 对象。
- 简略帮助、详细帮助、独立 HELP.md 和单双端包内使用说明同步更新。

## 验证结果

- `build.ps1 -BuildPublic` 成功；使用 `-O2 -Wall -Wextra`，本次编译无警告。
- 最终原生窗口回归 **627 / 627 通过**，覆盖真实 Windows 消息点击、悬停状态、展开 / 收起、误触取消、勿扰、禁用状态、邀请卡片、透明区域鼠标查询、四种 DPI、帮助 / 更新 / 战绩窗口和资源句柄。
- 最终报告：`dist/release-regression-a672b1dcb96a41be9f1ab96ed8232186/中文 用户路径/检查结果.txt`；同目录包含隐藏、默认、半展开、完全展开和其他窗口截图。
- 已直接读图检查默认操作栏、展开状态、滑动中间帧和简略帮助。窗口不跳动，文字无裁切，箭头和表情排列正确。
- 与 1.0.11 对比，96 / 120 / 144 / 192 DPI 下原四个图标显示区各 2208 像素 **全部一致**。按钮几何断言通过；不对背景云动画经过透明圆角时的像素要求静态一致。
- 四种 DPI 的游戏和历史战绩共 8 张截图 SHA256 与 1.0.11 相同，原飞机、爱心和窗口尺寸未受影响。
- `HELP.md` 与程序内五段简略正文、九段详细正文 **14 / 14 一致**，文本测量及导航检查通过。
- `verify_fix_regressions.ps1` 最终完整通过：客户端 / 服务端边界、29 项网关 / 统计测试、隔离本地完整对战、匹配 / 取消 / 断网清理 / 恢复 / 解绑 / 版本兼容、勿扰 / 表情、升级安装 / 回滚 / 哈希错误 / 中断恢复、单双端公网包静态检查。
- `tests/verify_embedded_components.ps1 -Version 1.0.12` 通过：启动器内嵌最新客户端 / 隧道 / 升级器，A 组爱心资源一致，C 拳头资源 162 与用户选定静态原图字节一致。
- `git diff --check` 通过。1.0.11 单端、双端 ZIP 哈希保持原值，旧包未被覆盖。
- 回归中遇到的旧测试问题已针对性处理：表情命中测试需要先设置展开状态；各 DPI 测试需要清除上一轮测试遗留的返回请求。没有为通过测试而放宽正式程序的状态防护。

## 交付文件

- 单端公网 EXE：`dist/PlanePet-Public-Single-1.0.12/PlanePet.exe`
  - SHA256：`a7599b0107292a764ed89dae87dcfe6e262cccf51aea86111bdf51f14fa973c8`
- 单端公网 ZIP：`dist/PlanePet-Public-Single-1.0.12.zip`
  - SHA256：`7c72571395b45726f9b2214cc26f16143664a34ffa3d27f52e92679adf3a4b55`
- 单机双端公网 ZIP：`dist/PlanePet-Public-DualLocal-1.0.12.zip`
  - SHA256：`a834407dc075c496fe7364a27ce645a536ebaa80918e8ae9b6f85bed5ad13467`

退出旧程序后启动新版。公网连接配置沿用原版，本次未发布在线升级清单，也未另做人工跨公网双人实战；本轮端到端回归使用隔离的本地服务器。EXE 与此前测试包一样尚未附 Authenticode 签名。以上是本轮验证范围，不构成对所有环境绝无问题的承诺。
