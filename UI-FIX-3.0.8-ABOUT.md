# PlanePet 3.0.8 关于页面交付记录

日期：2026-09-08。范围：Windows 客户端关于页、离线文档、菜单、构建与相关回归。不操作 ECS、OSS、GitHub 或 FingerKnight，不改变联网协议、战斗规则、绑定档案或统计授权。

## 已实现

- 桌宠与托盘共用的右键菜单新增“关于…”，位于“帮助与更新”之后、“退出”之前，旧功能与分组保留。
- 简洁主页：现有飞机图标、PlanePet、真实版本号、开发者 Ding、一句话介绍、反馈邮箱占位。
- `desktop/about_content.h` 中 `kFeedbackEmail` 留空；显示“暂未设置”，不创建空的 mailto 链接。
- “隐私说明 →”“第三方许可 →”在同一个非模态窗口内切换到阅读页；“← 返回关于”恢复小主页，Esc 在阅读页返回、在主页关闭。Tab、Enter、空格支持链接操作。
- 文档可滚动、选中、复制，不能通过键盘修改。返回入口固定，不随长文滚走。
- 主程序内嵌隐私文档及完整工具链许可，独立 EXE 不依赖外置 Markdown 或联网网页。
- 原始第三方素材授权核对待办保留到 `release/ASSET-LICENSE-CHECKLIST.md`，没有自动声明素材全部原创或擅自选择源码许可证。
- 隐藏桌宠、紧急隐藏、进入游戏、退出时会关闭关于窗口；关闭关于本身不会退出桌宠。不增加阅读统计。
- 字体按实际 DPI 绘制，窗口双缓冲且无刷新定时器；图标、字体与控件有明确生命周期。
- 随包说明同步关于入口，修正两处“原始安装凭据不会上传”的旧表述为“凭据经 TLS 传输用于鉴权，服务端持久保存摘要”。更新相应文案验证，防止旧错误再次进入包。

## 验证

- `build.ps1 -BuildPublic` 完整编译通过，单端/双端候选包版本均为 3.0.8。
- 实际 Windows 桌面隔离窗口回归：758 PASS / 0 FAIL，其中新增关于页检查 53 项，覆盖原生链接、只读长文、滚动、键盘、重复导航、关闭与游戏切换、GDI/USER 资源和 96/120/144/192 DPI。
  - 报告：`dist/release-regression-4c297d7f029747a6bb311b8bc5d73599/中文 用户路径/检查结果.txt`
  - 同目录保留主页、隐私首尾页、许可首尾页及不同 DPI 的实际窗口截图，已人工读图检查。
- 单独严格编译更新窗口（`-Wall -Wextra -Werror`）并执行既有窗口测试：55 PASS / 0 FAIL。
  - 报告：`dist/about-update-check-ac91cd7bf19f42c281b06ab21ee070ec/report.txt`
- `verify_fix_regressions.ps1` 通过：客户端/服务器边界、35 项网关统计测试、旧场景集成、匹配生命周期、恢复/解绑、版本兼容、更新安装/回滚/坏哈希/中断恢复、两个发行包检查。
- 最终重新构建后，单端/双端成品静态验证再次通过；内嵌客户端、隧道、升级器、既有素材及新增隐私/许可资源逐项比对源文件哈希一致。
- 内嵌许可文本 148506 字节、隐私文档 4094 字节；实际阅读器验证超过 32K 的完整许可不截断、末尾可达。
- 帮助内容一致性检查 18 段通过；PowerShell 语法检查、Git diff 空白检查通过。

首次受限桌面运行出现托盘不可用导致的两项旧测试失败，已在实际桌面环境重新验证通过。初次新测试还发现按 DPI 缓存图标造成资源计数增长，已改为窗口拥有并释放图标；“只读”测试改为验证用户键盘输入，而不是本来就允许程序调用的 EM_REPLACESEL。没有跳过失败项来宣称通过。

## 交付与边界

- 单端：`dist/PlanePet-Public-Single-3.0.8/PlanePet.exe`
- 完整包：`dist/PlanePet-Public-Single-3.0.8.zip`
- EXE SHA-256：`3304b575d5d0ee891582d610cd40086eacdf4e63fbf96742ac4bb2684d2725ae`
- ZIP SHA-256：`063036481e03fcfcd092ca58936026c5dd7631deffd69b517cfce56493329105`

按此前选择保留未签名候选包，未上传公网更新通道或仓库。本轮不是新的公网双机、跨设备长期运行或整体发布合规验收。反馈/隐私邮箱、服务器数据请求与保留规则，以及之前发布审查的其他未完成项仍需后续完成。更新状态测试旧的固定未来版本夹具不属于本次修改，本轮单独执行了更新窗口测试与升级器测试，不宣称整个 `verify_update_windows.ps1` 已全绿。
