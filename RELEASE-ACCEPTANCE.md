# Plane Pet 发布验收记录

## 当前正式版 1.0.0（2026-09-09）

当前构建、迁移、兼容性、公网部署和窗口检查的结果见 [正式版 1.0.0 验收记录](RELEASE-1.0.0.md)。本轮包括 783 项窗口检查、21 项更新状态测试、55 项更新交互测试、38 项网关/统计测试及六种旧存档组合；全部已执行通过。正式通道使用 epoch 1，旧测试通道保持不变。

历史测试版用户需手动安装一次正式版，保留 `%LOCALAPPDATA%\PlanePet`；不能用下文的 1.0.2 → 1.0.3 测试结论声称旧程序会自动降级安装正式 1.0.0。

未完成的独立双机、企业受限网络、长时间运行、实际跨卷/磁盘满、多屏拔插和生产容量验收仍然待测。Windows 发布者签名由开发者明确延后。运营、备份及 2000 连接方案见 [后续方案](RELEASE-1.0.0-OPERATIONS-PLAN.md)，不能把方案当成已部署功能。

## 历史记录：1.0.3 测试候选版（2026-09-06）

以下保留当时的记录，不代表当前正式版的编号或已完成所有独立环境验收。

状态只在实际执行后填写，不因“代码看起来正确”或“1.0.2 曾通过”标记通过。

## 自动化入口

1. `build.ps1 -BuildPublic`：完整编译客户端/服务端/隧道/升级器和单、双端包。
2. `verify_fix_regressions.ps1`：协议、配对生命周期/恢复/解绑、版本兼容、战局、网关和统计、升级安装/回滚。
3. `verify_update_windows.ps1`：更新状态机、关闭和迟到响应、窗口输入。
4. `verify_release_hardening.ps1`：130 项检查，包括中文参数与实际存档、托盘重建、二次启动唤回、就绪信号、诊断脱敏、四档 DPI、28 项二级菜单回归（层级/状态/命令/300 次资源释放）和 44 项邀请卡片布局回归（实际文字测量、边距、固定位置尺寸、表情栏恢复和生成图）。托盘测试需在登录用户的 Explorer 桌面运行；隔离桌面没有托盘服务不能判成代码失败或伪造通过。
5. `verify_game_render.ps1`、`verify_pet_visual_assets.ps1`：画面边界/像素资源检查。
6. `tests/verify_candidate_public_upgrade.ps1`：真实旧升级器→完整候选启动器/客户端/隧道，中文隔离目录和既有凭据副本，不改线上清单。
7. `tests/gateway_capacity_probe.py`：128 条本地连接、超额拒绝、3 波共 48 次重连和 2944 次回环；不测 ECS/TLS/游戏模拟容量。
8. `deploy/verify-restore-staging.py`：只在 ECS PlanePet 暂存目录启动备份副本，实际恢复已有绑定和后台。
9. `tests/public_latency_probe.py --allow-public-test`：双真实 WinHTTP 隧道的公网往返测试，既有 QA 凭据副本，无状态 nonce 探针，不创建绑定/上传统计。
10. `tests/public_windows_verify.py --allow-public-test --existing-qa-credentials --latency-client dist/PlanePetLatencyTest.exe`：测试客户端用 `PLANE_PET_LATENCY_SELF_TEST` 编译，仅测试构建加入 RTT 观测及自动移动。测真实消息泵、对局 RTT 和实际客户区，合成测试绑定结束后解绑，用户原档案不动。
11. `tests/verify_embedded_components.ps1`：只读比较两个 EXE 内嵌组件与本次构建的 SHA-256，防止修复没进入包内。

2026-09-06：上述构建和自动化已执行通过；另完成 Windows/Linux 备份回归、Linux 隔离编译与 26 项网关测试、现有统计副本的新旧读取兼容检查、现有凭据公网 TLS 隧道冒烟。具体范围和结果见 `FIX-VERIFICATION-1.0.3.md`，不等于下表独立环境全部通过。

## 需要独立环境的测试矩阵

| 环境/故障 | 预期 | 状态 |
|---|---|---|
| 两台独立 Windows 10/11 64 位电脑、不同网络 | 解压运行、绑定持久化、邀请双方同步、正常游戏 | 待实际双机 |
| 中文用户名、空格路径 | 存档/设置/战绩/升级路径无乱码 | 自动中文目录覆盖；独立中文账号待测 |
| 企业代理/认证代理/受限网络 | 游戏沿用直连；更新下载遵循系统代理；不修改组织策略 | 本机代理造成高延迟已复现并修复；企业环境待测 |
| 100/125/150/200% DPI、多屏拖动和显示器拔出 | 保持原紧凑物理尺寸及字体，清晰绘制，窗口可见，点击准确 | 130 项窗口回归含四档 DPI 及邀请卡片；前轮实际双端游戏客户区 240×372；真实混合屏、拔屏、极小工作区待测 |
| 桌宠/托盘二级菜单 | 一级七项、最多两层；全部旧功能保留，统计可关闭，危险操作仍确认 | 原生菜单结构/状态与命令路径通过，更新入口回归通过；真实悬停及边缘展开体验待人工确认 |
| Explorer 重启、重复打开、快捷键被占用 | 托盘重建、唤回实例、不误操作别的进程 | 自动消息回归；真实任务栏重启待测 |
| 下载中断、验签失败、文件占用/权限/磁盘满 | 不损坏旧版和绑定，提供原因 | 自动故障部分覆盖；磁盘满待测 |
| 新版初始化失败/无健康信号/断电恢复 | 回滚或保留旧版本备份，有失败说明 | 自动覆盖；实际断电不在用户机器上注入 |
| 实际 C/D 跨卷升级 | 同卷替换、安全回滚、资料保留 | 待实际跨卷 |
| Defender/SmartScreen/企业策略 | 已签名成品能清楚显示发布者，明确支持限制 | 有效证书及外部环境待确认 |
| 长时间可见待机/隐藏/追击/对战 | 分别记录 CPU、内存、GDI 句柄，无持续增长 | 可见/隐藏各 12 秒新旧对比已测；长时间与 GDI 趋势待测 |
| 并发与重连压力、备份恢复 | 预警和拒绝清晰，已有绑定不丢失 | 128 条本地隧道和 48 次重连通过；备份副本实际启动、3 对绑定恢复通过；生产完整链压测待测 |
| 现有单端 1.0.2 → 完整公网 1.0.3 启动器 | 新版实际初始化后确认成功，资料保留 | 真实旧升级器→最终启动器、客户端、隧道通过；合成档案保留、原始用户档案未改；未改稳定清单，线上检测下载推广仍待发布验收 |

不得为了测试停止同机 FingerKnight、清空生产统计、修改全局安全组或关闭 TLS 校验。需人工操作时先给用户明确步骤。
