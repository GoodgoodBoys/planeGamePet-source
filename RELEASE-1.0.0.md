# Plane Pet 正式版 1.0.0

日期：2026-09-09。开发者：Ding。反馈：planepet_public@163.com。

## 迁移说明

这是现有完整功能的正式编号重建，不是回退为早期测试版。历史 1.x、2.x、3.x 测试用户已经选择一次手动迁移：

1. 在旧程序右键菜单选择“退出”，包括单机双端测试实例；不能只关闭游戏窗口或隐藏桌宠。
2. 解压正式单端包，运行其中 `PlanePet.exe`。可以更换程序所在目录；同一 Windows 用户的 `%LOCALAPPDATA%\PlanePet` 必须保留。
3. 绑定、设置、统计选择、战绩继续从原目录读取。不要复制朋友的存档，不要将单机双端测试档案当成单端档案。
4. 好友也安装正式版后恢复兼容对战。旧程序不会自动安装版本号更低的正式 1.0.0，这是保留的安全限制。
5. 后续使用右键“帮助与更新 → 检查软件更新…”。可选更新拒绝后七天不自动提醒；手动检查不受影响；不兼容的大版本仍阻断对战。

EXE 已内置客户端、TLS 隧道、升级器、图片和离线隐私/许可文本，不需要另装运行库。建议分发完整 ZIP，保留外部帮助、许可及说明。当前未做 Windows 发布者签名，可能出现未知发布者提示；更新清单的 RSA 签名不代表已取得 Windows 发布者证书。

## 版本与数据兼容实现

- 正式版 `release_epoch=1`、显示版本 1.0.0；历史测试版 epoch 0。
- 配对 v4 仍为 48 字节鉴权封包，携带 epoch；对旧客户端保留 v3 响应。服务端同时按 epoch 和主版本判定兼容。
- 正式 1.x 显式映射既有 battle v3，鼠标/键盘同速、确定性同步及历史回放不因数字重置而退回早期逻辑。
- 统计库只增列 `release_epoch`，旧记录默认 0；不会把正式 1.0.0 误判为早期缺少结束原因的格式。历史记录不清零。
- 单端运行组件缓存改为 `runtime-release-1.0.0`，与历史同名测试版隔离；主存档目录及单实例互斥保留。
- 更新仅接收正式 channel/epoch、签名及摘要匹配的清单；双端测试包不能进入单端自动更新通道。

## 本轮已完成验收

| 范围 | 证据与结果 |
| --- | --- |
| Windows 编译 | `build.ps1 -BuildPublic` 成功，单端与单机双端 EXE 完整生成 |
| 核心回归 | 客户端边界、服务端、38 项网关/统计、旧设备场景、绑定取消/恢复/解绑、版本门禁、更新安装/回滚/中断恢复通过 |
| 手动迁移 | 合成旧存档的 3 种设置 × 2 种战绩格式，6 组合通过；绑定凭据与设置选择保留，不操作真实用户存档 |
| 更新窗口 | 21 项状态测试、55 项实际窗口交互通过；含关闭、迟到回调、静默七天、拒绝降级及等待空闲 |
| 全窗口复检 | 783 PASS、0 FAIL；报告 `dist/release-regression-918a1dac4495487c9a70c95732e633e8/中文 用户路径/检查结果.txt` |
| 静态发行包 | 两包通过；EXE 内嵌客户端/隧道/升级器、许可/隐私及选定素材逐项摘要核对通过 |
| 防误发 | 版本不符、非法版本、最低版本错误、未显式允许无发布者签名、双端误投、同版本不同内容等 11 项拒绝用例通过 |
| 清单密码学校验 | 真实客户端代码离线验证 RSA、SHA-256、长度、篡改拒绝、错误通道/epoch 拒绝通过 |
| ECS 部署 | 仅替换 Plane Pet 两个服务；历史统计逐行比较、SQLite integrity_check、绑定/凭据摘要与受保护配置/服务状态通过 |
| 公网链路 | 双隧道各 160 请求全部收到；中位数 31.09/28.93 ms，p95 33.03/30.59 ms；原档案摘要不变。这不是 2000 连接承载证明 |
| 公网配对版本 | 两条真实 TLS 隧道均通过 v3 旧版与 v4 正式版恢复响应检查；只查询随机不存在的绑定，未创建绑定/统计，追加的各 100 次就绪探针全部响应 |
| 干净目录重建 | 1311 个已审核源码/素材文件按摘要复制到全新目录，完整编译、嵌入组件及发行包检查通过。PE 带构建时间戳，重建字节摘要不同，未宣称逐位可复现；公开发布仍使用上文冻结的成品 |
| 专用续期 | dry-run、一次真实签发、实际服务证书加载与公网正常 TLS 验证通过；任务每小时执行且无截止日 |

新增迁移测试最初发现 Windows 保存时使用 CRLF，已按语义值校验修正测试；静态测试的旧版本/缓存目录默认值也已同步。未把测试夹具错误当作业务数据损坏，也未跳过失败项目。

## 成品与部署摘要

- 正式单端 EXE SHA-256：`7e366d251a1a87caf92fdd8ac059a5adddb35974688590a3d944362ea4688f2e`，17,510,519 字节。
- 单端完整 ZIP SHA-256：`16d0a503f79eddc235fd9354eed9e44b45b60d1bade6afb2f4df64db5eefa41a`。
- Linux 游戏服务 SHA-256：`c7bf1d02a96231d9b8c6032f74604eab9110d560a9885f9fb82d0e2dde1850d0`。
- 网关 SHA-256：`ee26382dde44490dc63209744e32b4a7802e2e4546613a5a40f72dbf5b98ee1a`。
- 部署备份：`/opt/plane-pet/backups/plane-pet-20260909T034403Z-4f377909`。只回滚本项目代码；不恢复共享服务器整盘。
- 新证书实际到期：2026-09-16 01:24:13（北京时间）；自动续期负责持续延长可用时间，不保证 CA、网络或服务器永久无故障。

## 发布状态与后续边界

OSS 正式目标为 `plane-pet/windows/release/`，保留旧 `stable/`；源码与发行文件分别归档至 planeGamePet-source 和 planeGamePet。以最后的线上复核记录为准，不能只凭本地签名文件推断已经上传。

第 5–7 项方案见 `RELEASE-1.0.0-OPERATIONS-PLAN.md`。外部告警、异机加密备份、服务器数据删除工作流及 2000 连接扩容尚未实施。Windows 发布者签名、独立两台干净 Windows/长时间验收及公开发行合规按开发者决定延后；自动测试通过不等同于所有系统无缺陷或已完成全部公众运营条件。
