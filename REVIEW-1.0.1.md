# Plane Pet 1.0.1 候选版：改动与出厂检查
日期：2026-09-09。结论：本次两项 Idle 改动可进入小范围体验；尚不应将“本轮测试通过”理解为已经满足无限量公开发行条件。

## 1. 已完成的改动

- 正常 Idle 悬停显示同排五个按钮：左侧“👊 开一局”，右侧大笑、大哭、生气、勾手四个表情；取消箭头、滑出动画及十分钟折叠状态。
- 移出约 0.4 秒后整排隐藏，保留防闪烁缓冲；重新移入全部显示。外部拖拽经过不会抢占操作。未绑定、等待邀请、对战及阻塞提示仍遵循原有状态限制，不开放无效操作。
- 悬浮窗仍为 280×150 物理像素。表情按钮 35×33，图像 23×24；底部留白 5 像素。游戏客户区仍为 240×372，顶部 HUD 52 像素在地图外。
- 在线两机改为连续、缓慢变形的环形曲线，覆盖全方向；红机领飞，蓝机跟随其先前经过的位置。位置及机头来自同一条曲线，不再使用强制向右的虚拟前进方向。
- 按用户确认，小窗口内允许转弯时缩短机距。997 种初始相位、每种连续 120 秒、每 16ms 采样：中心距 52.521–190.985px，单步最大 0.7326px，最大转角约 1.42°。这不是宣称在任意时长上做过穷尽验证。
- 气泡随两机左右关系放到外侧；状态徽标和点击区域采用相同方向。飞机换边时气泡会换侧，这是避免相互遮挡的布局取舍。
- 未改变离线单机飞行、自动射击频率、左右 15° 散射、战斗移动/伤害/同步、邀请和统计协议。
- 程序内简略/详细帮助、HELP.md、README 和打包使用说明均同步。18 个帮助章节逐段一致性检查通过。
- 本地候选版本为 1.0.1 / release_epoch=1，可与正式 1.0.0 保持小版本兼容；历史 epoch=0 测试版仍需手动迁移。

本轮没有连接 ECS/OSS、修改生产服务、清零数据、推送 GitHub，也没有修改 FingerKnight 或旧 ESP32 场景。1.0.0 的冻结 EXE 摘要复核未变。

## 2. 检查覆盖与证据

| 范围 | 本轮结果 |
| --- | --- |
| 完整 Windows 构建 | build.ps1 -BuildPublic 成功；客户端、服务端、启动器、TLS 隧道、升级器、单端和单机双端包生成；编译日志无警告 |
| Idle 路径 | 997 种相位通过；红蓝各覆盖八方向，机头与实际位移一致、历史路径跟随成立，24 小时及 uint32 最大时间点有限值/边界检查通过；双气泡矩形分离检查通过 |
| 真实 Windows 窗口 | 784 PASS / 0 FAIL；含四档 DPI、鼠标悬停、按下/释放、拖动、任务栏恢复、邀请卡、勿扰、历史、帮助、关于、隐私/许可子页及反复绘制的 GDI 资源检查 |
| 更新窗口 | 21 项状态测试、55 项窗口测试通过；关闭与迟到回调、七天免打扰、手动检查、不兼容门禁、游戏中延迟安装等 |
| 客户端/服务端回归 | 匹配取消日志、临时绑定过期/永久绑定保留、恢复/解绑、旧包拒绝、勿扰及表情、六组旧存档格式迁移、旧设备集成通过 |
| 网关/统计/运维单测 | 43 项 Python 单测通过：统计口径、epoch 兼容、鉴权/限流、损坏包、队列上限、SQLite 锁定隔离、过期事件清理、备份和续期逻辑；均为隔离测试，不是生产服务监控 |
| 运动与战斗核心 | 777,476 个运动断言、1,320,309 个战斗断言通过；同速、边界、双视角、预测/回放、精确接触、扣血时序、重复包和异常终止等 |
| 实际客户端弱网 | 本地真实双客户端注入 200ms RTT/2% 丢包测试通过；300ms/5% 条件下预期中止测试通过；倒数及对战阶段断网 7 秒再恢复，双方获知中止，战绩未被错误追加 |
| 实际渲染 | 对战接触/血量 54 帧校验；特效输出 204 张图，另有左右警戒渲染 400 帧检查；保留爱心裂开第五阶段、受损外轮廓、HIT 所属、WIN 循环/LOST 静态、结束动画播放和返回等 |
| 图像人工检查 | 查看实际程序输出的四个追逐关键时刻、工具栏、游戏 HUD/地图、历史和简略帮助；未见按钮挤边、双气泡互盖、HUD 遮挡地图、文字裁切 |
| 发行包静态检查 | 两包 PE x64 GUI、内嵌组件 SHA-256 与本次编译输出一致；隐私/完整许可/拳头/爱心/飞机/勾手43帧及战斗素材检查通过 |
| 发行防错 | 11 项预检拒绝用例通过；签名元数据 7 项守卫通过。未用模拟证书结果冒充真实 Authenticode 签名 |
| 补丁检查 | git diff --check 通过；本轮业务变更仅在桌宠、帮助及版本定义，服务端/协议/真实战斗源文件无改动 |

首次真实窗口跑测有一项鼠标悬停断言失败，其余项目通过。增加“实际指针到位、物理左键已松开、窗口可用”诊断后重跑全部通过；不能从重跑结果倒推首次失败原因已经确定。早期沙箱不具备实际 Explorer/鼠标环境的失败也未计作通过。

主要记录：

- [全窗口检查](<C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/dist/release-regression-a4e3618ca5064f45a9298f72efdfb574/中文 用户路径/检查结果.txt>)
- [核心回归](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/dist/regressions-1.0.1-final.log)
- [战斗与弱网](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/dist/battle-1.0.1.log)
- [特效](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/dist/effects-1.0.1.log)
- [更新窗口](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/dist/update-windows-1.0.1.log)
- [43 项 Python 测试](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/dist/python-audit-1.0.1.log)
- [飞行测试源码](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/tests/pet_flight_test.cpp)
- [隔离存储问题复现源码](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/tests/audit_storage_findings.cpp)

## 3. 当前确认的问题——尚未修复

### A. 战绩损坏可能静默清空，并在下一局覆盖原档案（优先级高）

位置：[LoadHistory / SaveHistory](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/desktop/main.cpp:2301)。

加载遇到非法头、汇总矛盾或非法生命值时执行 ResetHistory 后直接返回，没有隔离损坏文件、备份恢复或设置读取失败状态。之后一局的正常保存会替换旧档案。另一方面，部分截断尾行以 EOF 结束可能被当作正常读完，导致总数与最近列表不一致。

本轮用独立合成文件复现：原汇总为 20 局（12 胜、5 负、3 平），加入非法生命值行后，内存总数变为 0，persistence_ok 仍为 1；追加一局后文件变为“1 局、1 胜”，且没有 .bak。没有操作用户真实文件。

建议：区分首次无文件、读失败、格式损坏；完整解析到临时结构后再替换内存；保留损坏副本和最近有效备份；显示明确提示；未妥善保存原件前禁止用空历史覆盖。验收需补充半行截断、权限、磁盘满和原子替换失败。

### B. 本地统计 CSV 无法可靠区分历史测试版与正式版（优先级中）

位置：[LogEvent](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/desktop/main.cpp:2110)。

CSV 仅包含 timestamp_ms、client_id、app_version、invite_id、round_id、event、value。版本重置后，历史 epoch=0 的 1.0.0/1.0.1 与正式 epoch=1 的同名版本可能混合，影响导出分析。隔离测试实际生成 1000 行并确认缺少 epoch。

服务器上传包和 SQLite 已包含 release_epoch，所以这不是“服务器统计全部出错”。

建议：CSV 新格式增添 release_epoch/schema_version；保留旧文件并将旧行标为未知/旧格式，不凭数字版本猜测 epoch；同步导出说明与解析测试。

### C. 本地事件文件没有轮转上限，且在 UI 线程同步写入（优先级中）

位置同上。每条事件会检查文件、读取首行并追加；未见体积上限、保存期限或轮转。长时间开启统计会持续增大，本地磁盘/杀软扫描较慢时可能影响界面响应；本地写入失败也没有专门的可见反馈。

这属于代码确认的增长/阻塞风险；本轮未复现长期运行卡顿，也未将服务端 90 天清理误认成本地清理。

建议：有界队列、单独写入线程、按体积/时间轮转、持久化失败提示；关闭统计后不补写未授权活动。改动时仍须遵守当前隐私选择。

### D. 弱网检查/下载期间，退出可能等待网络超时（优先级中，静态风险）

位置：[同步 WinHTTP 调用](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/desktop/update_manager.cpp:202)、[Manager 析构](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/desktop/update_manager.cpp:424)。

析构设置取消标志后 join 工作线程；取消标志不能立即打断已阻塞的 WinHTTP 调用。接收超时配置为 15 秒，其余阶段另有超时，因此退出可能不是即时完成，不能简单保证“总是最多 15 秒”。

建议：采用可取消的请求生命周期/异步取消，安全关闭活动句柄并保证回调不访问已销毁窗口；用只读本地故障模拟验证解析、连接、接收和下载各阶段取消。当前更新交互回归通过，但未对全部真实网络阻塞阶段完成实操证明。

## 4. 上线缺口——不是本轮改动引入

### E. 正式 OSS 更新发布与新包端到端升级仍需完成

单端启动器指向独立的正式 release/latest.json。已有 1.0.0 交付记录明确标记 OSS 待发布；本轮没有上网重新判定它此刻是否已被其他操作补齐。可以确定本次 1.0.1 候选包没有上传或签发更新清单。

应按“版本文件及签名 → 校验 → latest 清单及签名 → 真客户端检查/下载/安装/恢复”的顺序验收；不可只上传 EXE 就宣布自动更新可用。升级组件本地回归已通过，不等于线上整链路通过。

### F. 当前配置不足以承诺 2000 同时在线，登记额度也会阻挡新用户

依据：[网关部署配置](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/deploy/plane-pet-gateway.service:19)、[登记/连接限流](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/gateway/plane_pet_gateway.py:802)。

仓库部署配置为 128 条并发连接、1000 份累计安装登记；同 IP 还有连接和滚动登记预算。不能仅把并发改到 2000 就宣称 CPU、带宽、Nginx、状态广播和数据库均满足要求。

匿名自助登记本身不是身份真实性证明；新身份可以累积占用有限登记额度。现有限流有作用，但不能承诺防止分布式或长期消耗额度。满额后已有身份与新用户行为需要分别处理，不能通过清空永久绑定腾空间。

本轮未进行生产压测，也未擅自加资源/改限额。应按既有[容量方案](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/RELEASE-1.0.0-OPERATIONS-PLAN.md)在独立环境逐级压测和防滥用验证。小范围测试可控放量；2000 人目标是单独验收条件。

### G. 服务端资料处理及运维闭环尚未完整上线

[隐私文档](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/PRIVACY.md)当前明确：本地清除不删除服务器数据；必要联网身份、运维日志、备份的统一保留/删除流程仍待完善。

需要落实身份验证后的数据请求流程、处理审计和备份清理规则。已有续期与备份代码/测试，不等于已有独立外部告警和异机加密恢复保障；应继续落实证书失败、可用性、磁盘/SQLite异常、异机备份和恢复演练。此处是工程检查，不作法律合规结论。

### H. 真实发行环境验收尚有边界

本轮覆盖当前这台 Windows、隔离模拟档案、自动化窗口及本地故障注入，未完成：

- 两台独立干净 Windows 10/11 的首次启动、真实跨运营商对战和互相升级；
- 办公代理/受限网络、实际杀软与 SmartScreen 行为；
- 多显示器混合缩放热插拔、休眠恢复及长时间持续运行；
- 大规模并发、生产故障告警及异机恢复全链路。

当前仍按开发者决定不做 Windows 发布者签名，静态检查明确为 NotSigned；可能出现未知发布者提示。这是已接受的分发取舍，不应再称为“签名已完成”。建议完成基本双机矩阵和小范围灰度后再逐步放量。

## 5. 建议处理顺序

1. 先修复 A，避免用户战绩被覆盖；同步补齐 B、C 的本地统计存储质量。
2. 修复/实测 D 的取消与退出路径。
3. 确认候选体验后再完成 E；本轮不会自动发布尚有已知问题的新版本。
4. 面向公众放量前落实 G、H；达到 2000 客户端目标前必须完成 F。
5. 不以“所有自动测试都绿了”替代用户验收和生产运行证据。

## 6. 本地候选交付

先从旧程序右键选择“退出”，再运行需要的包；单端和双端测试版不要同时启动。绑定、设置和战绩无需清除。

- [单端公网 PlanePet.exe](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/dist/PlanePet-Release-Single-1.0.1/PlanePet.exe)
- [单端公网完整 ZIP](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/dist/PlanePet-Release-Single-1.0.1.zip)
- [单机双端公网完整 ZIP](C:/Users/81228/Desktop/alive/ProjCode/V1_1PFIO/plane_link/pc_pet/dist/PlanePet-Release-DualLocal-1.0.1.zip)

SHA-256：

- 单端 EXE：fe28170e88133dd09fc7181a4258bec17e650e54eddc97145e1d71eecb3e46e6
- 单端 ZIP：9686e6be5899151517c0e946c01ade13f1eb7ab6a18d51cc95b75e2b5e290b6d
- 双端 ZIP：58bb123155695a0b2846b500933d604285a464b3f34e2c75379481b9f029b938
- 归档 1.0.0 EXE 复核未变：7e366d251a1a87caf92fdd8ac059a5adddb35974688590a3d944362ea4688f2e

不提供“绝对无 bug”保证；本报告区分了通过的检查、已确认问题和仍未完成的验收。
