# 1.0.7 历史战绩图标与排版

2026-09-06。仅本地客户端展示调整及打包；未部署 ECS、OSS 或 GitHub，未操作 FingerKnight，未读写用户真实战绩。

## 修改

- 历史列表的“我 / 对”替换为现有 D2 蓝 / 红飞机资源；两者均绕模型中心逆时针旋转 45°，机头朝左上。采用 28px 逻辑尺寸，沿用历史窗口既有 DPI 缩放。
- 每侧固定显示 3 个爱心位置。剩余血量使用战斗 HUD 的蓝 / 红配色，已扣除血量保留同形状灰色槽。
- 复用 HUD 爱心的双圆瓣与底部尖角几何。战绩栏扩大后采用区域合并再描外轮廓，避免各部分内部描边形成横线；游戏 HUD 默认绘制分支不变。
- 时间由 `09-06 19:33` 改为 `09-06 19 : 33`，仅改变显示字符串，不更改时间戳、时区转换或存档格式。
- 历史窗口尺寸、最近十条记录规则、胜负平统计、存档和其他窗口均保留。

主要文件：`desktop/main.cpp`、`desktop/history_layout.h`、`common/app_version.h`。
新增回归：`tests/history_window_cases.h`，由 `tests/release_window_cases.h` 调用。

## 验证

- 最终 `build.ps1 -BuildPublic` 通过，编译参数包含 `-O2 -Wall -Wextra`。
- 最终原生窗口回归 **456/456 通过**：包含 96/120/144/192 DPI 下十条记录与空历史、红蓝模型/方向、0..3 血量和彩色/灰色心形槽、时间宽度、窗口打开/关闭，以及原有桌宠/邀请/勿扰等检查。
- 新增 20 次重复绘制的区域/画刷 GDI 句柄检查，通过。
- 以独立临时存档验证战绩保存/重新加载、胜负平统计和最近十条保留，通过。
- `verify_fix_regressions.ps1` 通过：客户端/服务端边界、29 项网关/匿名统计、本地完整对战、匹配生命周期、版本兼容、勿扰、表情、升级安装/回滚/恢复和两种 ZIP 静态检查。
- `verify_game_render.ps1` 已通过；最终原生窗口中的战斗图像与 1.0.6 在四种 DPI 下 SHA256 均完全一致，确认游戏 HUD 外观未改变。
- 最终单端/双端内嵌客户端与隧道、单端升级器哈希均与本次编译产物一致。
- 已按原始尺寸查看历史完整十条、空历史和不同 DPI 的图像。最初的模型检测因颜色阈值过窄及以逻辑坐标跳过高 DPI 物理像素而误报；改为遍历物理像素和识别橙黄色范围后全部通过，未移除方向断言。
- `git diff --check` 通过。没有重复跨公网人工双人实战或线上更新发布；协议、联网配置和统计逻辑本次未修改。

最终报告与图像：

`dist/release-regression-05125a3d01f54f31af8e81e3f490d861/中文 用户路径/`

预览 `history-120.png` 为 125% 缩放、合成测试战绩，不是用户存档。

## 交付

- 单端公网 EXE：`dist/PlanePet-Public-Single-1.0.7/PlanePet.exe`
- 单端 ZIP：`dist/PlanePet-Public-Single-1.0.7.zip`
- 单机双端公网 ZIP：`dist/PlanePet-Public-DualLocal-1.0.7.zip`
- 保留旧 1.0.6 包；退出旧程序后运行新包，可沿用绑定与战绩。新 EXE 仍未附 Authenticode 签名。

SHA256：

- 单端 EXE：`f335429f2413318749a436146c8d3788e785cc96a2dd7204f8c6324a76a2aa34`
- 单端 ZIP：`9852d11e8e7afd88e0a850e93e2dc93f1b8026d129ea51cf3d49b9128cc0eb8f`
- 双端 ZIP：`601ecdd568f77514c2b8387bac6ae490ed517697042d77a80898a987f21b58dc`
- 内嵌客户端：`f6d8c5fa21a005693478a07d29668bed75e991b51c6df4ac162021cb42e558d8`
