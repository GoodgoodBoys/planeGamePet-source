# 1.0.14：撤回左键射击，恢复自动随机射击

2026-09-07。按本次要求只撤回左键射击相关功能；1.0.13 的表情展开记忆完整保留。未修改服务器、GitHub、OSS、FingerKnight 或真实用户存档。

## 行为

- Idle 蓝机持续自动射击，恢复约每 620 毫秒一发、子弹存活 1120 毫秒的节奏。在线跟随红机和离线单机飞行均保留。
- 每发子弹相对发射瞬间机头，在 -15° 至 +15° 内随机偏转；起点为当时机头，发射后保持方向、匀速直线飞行，不在每帧重抽角度。
- 每次启动随机生成种子，每发子弹有固定序号；画面重绘、点击和拖动不会改变同一发子弹的方向，也不会增加子弹。
- 已删除左键射击入口、鼠标瞄准、手动子弹列表和专用透明点击层。透明空白区恢复原来的点击穿透，左键拖动飞机保持可用。
- 射击只是本机 Idle 特效，无伤害或击落，没有新增联网动作或统计事件；真实对战的射击规则和子弹颜色未变。
- 表情栏移出后隐藏并记住展开状态，连续 10 分钟未有效发送表情才收起；发送后重新计时。按钮尺寸和表情比例不变。
- 程序内帮助、HELP.md、README 和单双端包内使用说明已移除旧的“点击射击”操作描述。

## 验证

- 编译：`build.ps1 -BuildPublic`，`-O2 -Wall -Wextra`，通过。
- 原生 Windows 界面检查 **672 / 672 通过**。
  - 报告目录：`dist/release-regression-d8d6bd40860949e58e51f7e3c5ee2bda/中文 用户路径/`。
  - 10000 发样本验证角度始终在 ±15° 内、左右都有分布、不同种子产生不同序列、重绘保持同一角度。
  - 361 种机头方向验证相对角度限制及单位速度；验证发射节奏、生命周期、机头位置、直线轨迹以及长时间隐藏后无集中补射。
  - 多次左键点击、拖动和鼠标捕获丢失均不改变同一时刻的自动子弹序列；拖动位置正常。
  - 96 / 120 / 144 / 192 DPI 下确认恢复 LWA_COLORKEY、真实 OS 空白命中穿透、窗口仍为 280 × 150。
  - 原有表情记忆及有效发送 / 冷却 / 离线计时检查全部保留并通过。
- 已读图检查自动子弹、表情展开栏及简略帮助；未出现文字裁切或窗口尺寸变化。
- 四种 DPI 下的表情图标显示区域与 1.0.13 逐像素一致；游戏、历史战绩共 8 张截图 SHA256 与 1.0.13 相同。
- `HELP.md` 与程序内 14 段帮助正文一致。
- `verify_fix_regressions.ps1` 完整通过：客户端 / 服务端边界、29 项网关与统计测试、隔离本地完整对战、匹配生命周期、恢复 / 解绑 / 版本兼容、勿扰 / 表情、升级安装 / 回滚 / 恢复及单双端公网包静态检查。
- 既有飞机、云、勾引动画等素材检查通过；C 拳头及 A 组爱心资源保留。
- 静态搜索确认正式代码不再包含旧的 FireIdleShot、petClickCanShoot_、PresentPet、idleShots_、IsIdleBlankPoint 路径；`git diff --check` 通过。
- 1.0.13 单端和双端 ZIP 保持原哈希，旧版本未覆盖。

## 交付

- 单端公网 EXE：`dist/PlanePet-Public-Single-1.0.14/PlanePet.exe`
- 单端公网 ZIP：`dist/PlanePet-Public-Single-1.0.14.zip`
- 单机双端公网 ZIP：`dist/PlanePet-Public-DualLocal-1.0.14.zip`

退出旧版后再打开新版。本次未发布在线升级、未重复人工跨公网实战；回归联网使用隔离本地服务。EXE 仍未附 Authenticode 签名，验证结果不代表对所有环境绝无问题的承诺。
