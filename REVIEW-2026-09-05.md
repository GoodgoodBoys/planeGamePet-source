# 更新窗口关闭修复与代码复检（2026-09-05）

## 已复现的关闭缺陷

关闭按钮执行了 DestroyWindow，但 Manager::Dismiss 仅切换到 Idle，保留了
manual/required 标志并增加 generation。主循环随即因 manual/required 再次
调用 ShowUpdateWindow，因此看起来 × 没有响应。

修复前：专用原生窗口回归连续 6 次记录 x_destroys_window=PASS、
x_stays_closed=FAIL；检查中关闭、WM_CLOSE 关闭也失败。
修复后：同一组回归全部通过。

## 本次代码修复

- 关闭时清理提示意图、版本展示内容和安装就绪状态；Idle/Disabled 永不弹窗。
- 后台成功、错误和安装请求的发布，与取消共用锁，避免关闭后迟到结果覆盖状态。
- 已取消的网络线程结束前，新的手动检查排队，避免复用取消标志使旧请求复活。
- 手动点击可接续正在进行的自动检查；手动大版本检查保留强制联机兼容提示。
- 新版本未发布时不提供同版本或降级下载操作。
- 更新提示及安装在邀请/对局期间延后，返回待机后继续；关闭可撤销待安装状态。
- 增大 × 的点击范围、加入 Esc/关闭/取消下载；按下与松开必须命中同一操作，
  避免拖出按钮或状态变化后误触其他操作。
- 避免状态展示刷新造成更新统计重复计数；统计字段和服务端协议保持兼容。
- 修正下载长度上限的无符号减法边界，拒绝最低支持版本高于最新版本的清单。
- 主窗口退出后停止后续循环处理；帮助说明补充关闭与取消下载操作。
- 保留上一轮已完成的双缓冲和按需定位/重绘修复。

## 验证结果

以下在修复代码的构建上通过：

- build.ps1 -BuildPublic：Windows 客户端、服务器、隧道、启动器、更新器及测试程序编译通过。
- verify_update_windows.ps1：全部更新状态关闭、迟到成功/错误/安装、立即重新检查、
  7 天免提醒、强制版本、鼠标命中/移出、Esc、邀请/对局延后、隐藏后迟到响应。
- 原生 InfoSelfTest：帮助、战绩、更新窗口属性，结算关闭返回桌宠、战绩持久化。
- UDP IntegrationTest：匹配、邀请、取消、接受、倒计时、超时平局、返回、表情、防重放。
- PairingLifecycleTest：取消、等待清理、匹配码复用、永久绑定存储。
- VersionCompatibility：匹配/恢复阶段的大版本信息与兼容标志。
- verify_gui_flow.ps1：首次匹配、隐藏端唤醒、重新启动后的绑定恢复。
- verify_restart_regressions.ps1、verify_invite_timeout.ps1。
- 游戏布局、双方坐标对称、边界渲染、桌宠资源和追击动画检查。
- OSS 真实更新清单读取及签名验证：latest=1.0.0，signed=1。
- verify_updater.ps1：成功安装、失败自动回滚。
- 网关匿名统计单元测试 7/7，含旧版本统计与更新事件共存。
- 单端/双端公网发布包静态检查、git diff --check。

实际 Windows 界面工具未枚举到独立验证桌宠，因此未完成截图及物理鼠标复核。
关闭验证由专用程序创建真实 Win32 窗口，发送按下/松开、WM_CLOSE 等消息，
持续运行实际主循环，检查窗口不重新出现。测试接口不编译进交付程序。

## 独立待处理项（静态发现）

系统 MessageBox、GetSaveFileName 与 TrackPopupMenu 使用嵌套消息循环；当前
网络收发和心跳在外层主循环调用 PetClient::Update，没有 WM_TIMER 服务。
因此，确认框/保存框/右键菜单长时间保持打开时，网络更新暂停，可能超过服务端
10 秒失联阈值，引发好友离线或对局结束。此次非模态更新窗口不受该问题影响。
建议后续为这些原生模态交互增加受控的网络定时服务，并防止其间安装更新或
重复进入状态转换。这一独立消息循环调整尚未包含在本次修改中。

## 交付

本地重新生成的单端包：dist/PlanePet-Public-Single-1.0.0.zip。
SHA-256：716c180f4e837ba33c956f01367dc988d73c72517a908068aae445be6cc1db16。
双端包也已重新生成。版本号仍为本地验证用的 1.0.0；GitHub 和 OSS 本轮未发布。
退出旧程序后使用新包。正式上线时应另发补丁版本，避免线上同版本二进制不同。
