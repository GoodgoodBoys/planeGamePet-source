# PC 战斗同步 3.0.0（实施与验收记录）

状态：2026-09-07，开发、构建、自动验收与公网部署通过；提供 3.0.0 验收候选包。真实人工鼠标体验仍待用户确认，尚未发布 GitHub/OSS 更新清单。不能把这一未完成项算作通过。

## 边界

- 只修改 PlanePet PC—PC；保留 2.0 移动协议与 ESP32/shared 原有规则。
- 3.0 使用独立消息类型及双方大版本门禁；绑定、历史文件和统计数据库不迁移或清空。
- 服务器部署只允许 PlanePet 自身目录/服务；保护 FingerKnight、旧设备服务、Nginx 和现有配置。
- 不发布 GitHub/OSS 自动升级清单，先交付单机双端公网候选包供验收。

## 实施设计

1. 本局输入编号等于模拟 tick。服务器只确认双方真实输入齐备的同一 tick；短暂缺帧保留整个世界，恢复后在真实时间预算内重演积压的完整游戏步，而非只加速玩家。确认滞后有上限，超过则本局中止。
2. 客户端统一预测飞机、自动开火、子弹与碰撞，保留自己的真实输入；对方输入经服务器转发，短期未知输入使用最近有效输入预测。每次确认从完整权威状态重演未确认步。
3. 输入以固定容量历史保存，首份有效输入不可被改写；限制可接收的未来 tick、重放窗口和工作量。迟到数据不能改变已确认结果。
4. 子弹使用固定出生 tick/身份与精确坐标；明确保存命中 tick、步内时间、接触位置、目标和扣血后状态。事件重传去重；结算只来自服务器。
5. 绘制前后完整预测世界，避免混用即时蓝机、旧子弹和先到的血量；处理新弹、消失弹及终局的最后一次碰撞，不以改变飞机速度掩盖错误。
6. 保留现有加密 WSS 公网通道，本轮不扩大到网络基础设施或加密传输协议迁移。

## 验收结果

- [x] 新核心 1,320,243 次断言：统一限速、完整重演、输入不可改写、同一步双方伤害、精确连续碰撞、事件去重/乱序/不可改写前缀、180 秒上限、序列化边界和随机畸形载荷。
- [x] 原 2.0 核心 777,476 次断言通过；PC 旧协议和 ESP32/shared 文件保留。
- [x] 网络模拟：RTT 0/50/100/200/300ms、上下行 20/160ms 非对称、20/35ms 抖动、2%/5% 丢包、200ms 短暂断流、30/60/120 FPS；固定 60Hz 模拟。确认队列可恢复，不持续积压。
- [x] 实际渲染器加载嵌入飞机/爱心，生成两端视角、静止及六种移动方向的命中前/后/帧末共 42 张图像，校验血量与命中步内时间；已人工读图检查代表帧和真实网络对局的接触截图，不把全部图像生成等同于全部人工浏览。
- [x] 真实 Windows 双客户端经本地弱网代理完成对局；测试驱动调用生产鼠标目标路径，检查每次位移不超过执行模拟步的速度预算。公网 WSS 双端完成正常对局和故障中止；两端最终结果、HP 一致。
- [x] 窗口专项 702/702 通过，包含菜单、邀请卡片、DPI、托盘、表情、帮助、历史、返回桌宠、更新弹窗等。原配对生命周期、大小版本兼容、勿扰、旧场景完整对局、29 项网关/统计回归、升级安装/失败回滚/中断恢复通过。
- [x] Windows 与 Linux 严格编译 `-Wall -Wextra -Werror`，Linux 核心额外使用 UBSan；Windows 全量 `build.ps1 -BuildPublic` 通过。
- [x] 单端/双端 ZIP 结构、GUI 子系统、EXE 3.0.0 版本信息、嵌入客户端/隧道/更新器哈希、飞机/拳头/爱心资源、18 个帮助段落同步检查通过；`git diff --check` 通过。
- [x] 部署和所有公网测试结束后，受保护文件及其他服务 PID 一致；绑定与网关凭据文件哈希、统计行数均与备份一致，数据库 integrity_check=ok；临时 QA 凭据副本及合成绑定已清理。
- [ ] 人工握持鼠标的主观验收：computer-use 控制器的窗口清单未暴露本程序的 `WS_EX_TOOLWINDOW`，无法选中进行真实点按/拖动。两次隔离普通客户端启动尝试后停止，未通过非支持方式注入鼠标。第二次正常生产客户端已自动完成一局且两端各保存一次平局，但不替代人工操作确认。

### 最终实测证据

目录均相对于本项目：

- `dist/battle-verification-6a4da41c383a4bd18227e721ff3d1ab3`：最终 42 张渲染帧及 contacts.csv。
- `dist/release-regression-971430a9437c4245976807b3c99e857e/中文 用户路径/检查结果.txt`：702 项窗口回归。
- `dist/motion-live-6525e264a5aa41088968db1bb2e5e203/result.json`：注入 200ms RTT、2% 丢包，两端正常结束，最大未确认 23/22 ticks。
- `dist/motion-live-4cc45efa9fbe43b7bd490b7246a30daa/result.json`：300ms RTT、5% 丢包及客户端停顿，明确中止，不计胜负。
- `dist/motion-live-ea847aa607b24e91944844442f9f9fca/result.json`：最终公网正常对局，最大未确认 14/14 ticks，双方 HP 0/0、平局；同时保存鼠标路径、接触距离 CSV 和真实绘制 BMP。
- `dist/motion-live-6058b32e5dbe47ecbc40c6d9751e7b73/result.json`：最终公网同步中止，双方 HP 3/3，历史计数不增加。
- `dist/motion-public-7a65152cd19c42cd919df9dd10a21f41/safety.json` 与 `dist/motion-public-c1bc4959ed4a445fba67b1e286d0e02c/safety.json`：原用户档案未变，QA 凭据副本已移除。

测试遇到的问题均保留记录：窗口自检最初在沙箱内出现托盘/鼠标查询失败；同一代码在实际桌面权限下两次 702/702 通过。Linux 测试的无符号整数窄化已显式转换。战绩测试夹具的上一局去重标志已重置，未为测试修改生产战绩规则。一次重建因本次验收客户端占用 EXE 失败，关闭自有测试实例后全量重建通过。

## 发行与回退

- 双端候选：`dist/PlanePet-Public-DualLocal-3.0.0/PlanePet.exe`，压缩包同名 `.zip`。用一台电脑经公网测试；请先退出旧双端版以释放本地隧道端口。
- 单端候选：`dist/PlanePet-Public-Single-3.0.0/PlanePet.exe`，压缩包同名 `.zip`。两台电脑需都运行 3.x 才能使用新战斗；3.x 与 2.x 不能对战，旧绑定无需删除。
- 最终客户端 SHA256：`41d55ef9a8981a167a2543e0b17083df7e4aae1e8f0b2f4388e7e6d84b111daa`。
- 双端 ZIP SHA256：`f8f9e91f5be8d1c691d141f1e8f599352fb40da41baafc387c0ccaab183df102`。
- 单端 ZIP SHA256：`9c8565a2806b345938e1b6d2e4fe2014f050176a8e149737ac944bf0ab74736a`。
- ECS `/opt/plane-pet/bin/plane-pet-server` SHA256：`ad2f901ccc9cb4ac93cfbed7a695eb4d3c0608a7fc5c28749b6b7a2a0a17c439`；最终头文件重编译后的服务二进制仍与线上相同。
- ECS 回退备份：`/opt/plane-pet/backups/battle-3.0.0-5q1mjfaj`。只替换 PlanePet 服务二进制，重启其自身与有依赖关系的网关；网关代码/配置未变。回退应先确认无在线用户，不恢复或覆盖其他项目及用户数据。
- 修改前本地源码和程序备份：`dist/before-battle-3-7cacefc99d4f4809a5ca5de2b6935262`。原有 2.0.0 发布包保留。
- 本包仍未做 Authenticode 签名，属于验收候选，不宣称已通过正式发行签名门禁；没有推送 GitHub 或 OSS 自动更新清单。

## 复验入口

1. `build.ps1 -BuildPublic`：先全量构建；不要同时运行会占用输出 EXE 的测试客户端。
2. `verify_battle.ps1`：核心、旧协议、42 张渲染图、本地双客户端弱网/故障测试。
3. `verify_battle.ps1 -AllowPublicTest`：另外通过现有 QA 凭据执行公网测试；涉及网络授权，不创建新账号，不上传统计。
4. `verify_fix_regressions.ps1` 与 `verify_release_hardening.ps1`：既有功能、更新和窗口回归；后者需实际 Windows 桌面权限，避免将沙箱 API 限制误判为产品故障。
5. `tests/verify_embedded_components.ps1 -Version 3.0.0` 与 `tests/verify_help_content.ps1`：嵌入资源及帮助一致性。

人工最后验收建议：在双端公网包内开局，分别测试慢拖、快速左右反向、迎着子弹移动、远离子弹移动及切换键盘；观察是否出现蓝机突然跳到鼠标、明显未接触机身即扣血。结束后点画面/按键/关闭按钮应返回桌宠。若异常，请保留发生时版本、两端延迟及录像，以区分帧率停顿、远端预测修正和真正时序错误。

## 已知物理边界

预测不能获知尚未到达的对方输入。远端急转或极端网络下可能修正未确认的表现；不能承诺任意延迟下完全零修正。超出窗口明确中止，不伪造命中或永久追赶。

画面中的 HP/接触反馈参与预测，必要时会按权威结果修正；胜负、最终 HP、历史和统计结算以服务器为准。服务器不会重写已确认 tick，而是等待两份输入齐备后确认完整世界。确认/预测窗口上限 60 ticks（1 秒），本地消息泵停顿超过 250ms 中止；低帧率或系统长时间卡顿的观感不等同于网络无指令瞬移。

几何部分独立按现有 36px D2 素材轮廓校准，排除透明机翼上方和尾焰，并以彩色弹头接触为碰撞点；没有缩放模型或用碰撞半径抵消网络延迟。窗口仍为 240×372，HUD 52px 独立在地图外；共享旧仿真、布局和帮助内容哈希与修改前一致。
