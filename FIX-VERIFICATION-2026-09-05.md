# Plane Pet 1.0.1 — 确认问题修复与验收记录

日期：2026-09-05；公网验收续检至 2026-09-06。基于原工作区继续修改，保留此前更新窗口修复及所有无关改动。已限定范围更新 ECS 桌宠服务；没有提交 Git、上传 GitHub/OSS、清零正式统计或修改 ESP32 ↔ Windows 场景。

## 交付状态

本地源码修复、Windows 编译、隔离功能回归和 1.0.1 打包完成。ECS 的 Linux 编译、21 项网关/统计测试、正式数据副本的新旧版本兼容性验证、限定范围部署以及 Windows 双端真实公网验收均已通过。同机 FingerKnight 和原设备互通服务未修改。下表所列服务端上线依赖现已完成，详细证据见文末。

单端测试包：`dist/PlanePet-Public-Single-1.0.1.zip`；解压后运行 `PlanePet.exe`。双端公网测试包：`dist/PlanePet-Public-DualLocal-1.0.1.zip`。两者仍指向原 ECS WSS 地址，没有替换成局域网地址。包内不含个人绑定、统计数据库或全局服务器密钥。

## 与上次审查逐项对应

| 编号 | 针对性修复 | 验证与上线依赖 |
|---|---|---|
| F01 | 请求日志跨重启保留取消意图；等待取消确认；撤销未完成的临时绑定；双方未确认的临时绑定 60 秒过期 | 取消与撮合竞争、重试、日志确认、单边确认超时与永久绑定保留测试通过；完整保障需要新服务器 |
| F02 | PC 动作附邀请/对局、阶段、操作 ID；服务器确认、去重并拒绝过期上下文；客户端清理失效重试 | 真实 UDP 注入旧 Accept 后仍保持新邀请等待，新的 Accept 正常进入倒数；需要新服务器 |
| F03 | 状态回填绑定 ID，错误只关联当前请求；解绑/重绑后轮换恢复请求标识 | 旧 Unbound、旧 BindingMissing 不清除新绑定；新服务器关联字段更完整 |
| F04 | 下载后复制到程序同目录，再校验、刷新文件并同卷替换 | 安装/回滚通过；实际 C→D 两物理卷未执行，跨卷复制流程已静态复核 |
| F05 | SQLite 写入和后台汇总移入单独工作线程、有界队列；异常统计丢弃而不关闭游戏连接 | 21 项网关/统计测试通过，包含真实本机 WebSocket→UDP 在数据库写锁与错误统计下继续往返；需要部署网关 |
| F06 | 完整 WebSocket 握手验证先于凭据登记；只信任回环代理的末端来源；Nginx 覆盖 XFF | 非法握手不占名额、伪造来源不能绕过、凭据保存失败不登记测试通过；需要部署网关及代理配置 |
| F07 | 每来源登记改为每小时配额；默认 64 条同出口隧道、每安装 2 条；回环 UDP 限流按端点分桶 | 45 个独立端点全部响应；小时配额和安装连接限制测试通过；需要部署服务器、网关和代理 |
| F08 | 好友版本或大小版本要求变化时重新判定提示，不沿用旧的“已处理”状态 | 小版本提示之后变成大版本不兼容、七天免打扰与强制提醒测试通过 |
| F09 | 仅 idle 状态检测表情按钮点击 | 接受按钮下缘不再被隐藏表情区域吞掉，中心点击仍有效 |
| F10 | 设置/清理失败明确提示；关闭统计有独立持久化标记；战绩清除失败恢复界面原数据 | 文件占用下关闭统计后重读仍关闭；清理失败不报告成功且保留战绩 |
| F11 | 倒数前清零对战起点；未进入 Playing 的时长为 0；统计分母包含倒数开局并去重 | 倒数断线返回 0；孤立结束事件不会令完成率超过 100%；服务器及网关需部署 |
| F12 | 只累计开启统计期间时长，暂停后重新开启继续累计；旧版本采用可观测间隔保守计算 | 开启区段、旧格式兼容与 25 小时会话测试通过；新汇总需部署网关，历史精确时长无法凭空恢复 |
| F13 | 独立每分钟清理过期事件，后台汇总前也做清理；安全删除及 WAL 检查点 | 模拟 91 天无新事件，旧记录从表和汇总中删除；需要部署网关 |
| F14 | 早期安装失败重新打开旧版并给出原因；状态日志、原子替换、异常中断恢复；启动器管理自有子进程 | 正常安装、不健康新版回滚、坏包校验、日志写入失败和中断恢复通过；子进程 Job 管理已静态复核 |
| F15 | 游戏窗口按桌宠所在显示器的工作区定位 | 布局与代码静态检查通过；本轮未执行真实双显示器移动操作 |

前轮已知的模态窗口阻塞也一并处理：主窗口定时泵维持联网；重入保护防止递归更新；原生菜单、确认框、导出框打开期间禁止触发升级安装。

## 已执行检查

- 最终 `build.ps1 -BuildPublic` 成功；使用 C++17、`-Wall -Wextra`；`git diff --check` 无错误。
- `verify_fix_regressions.ps1`：客户端 8 项边界检查、服务端临时绑定过期、21 项 Python 网关/统计测试、取消竞争/延迟 Accept/断线时长/端点限流、旧协议完整对战、首绑/存档恢复/解绑/版本兼容、升级器 5 条路径、公网包静态检查。
- `verify_update_windows.ps1`：16 项更新状态测试和真实窗口过程回归，包括连续开关、× 边缘点击、拖出取消、Esc、迟到工作线程、邀请/游戏暂缓与回到 idle 后恢复提示。
- `verify_gui_flow.ps1`：首绑、隐藏对端唤醒、重启自动恢复通过。
- `verify_restart_regressions.ps1`：旧对战不会在重启后恢复、战绩不重复、对端收到结束原因通过。
- `verify_invite_timeout.ps1`：超时反馈、双方回 idle、未回答邀请不计入战绩通过。
- 帮助/历史/更新窗口内置自检通过；结算关闭与历史持久化通过。
- `verify_game_render.ps1` 和 `verify_pet_visual_assets.ps1`：状态栏 52 px 与完整 320 px 地图区分离、双方边界/命中/移动对称；两机追逐、D2 模型、三种云和四种表情保持；勾手 43 帧/2150 ms。
- 已读图复核 `dist/game_boundary_fixture.bmp` 与 `dist/pet_online_fixture.bmp`。computer-use 未列出可操作的隔离桌宠工具窗，本轮没有冒称完成外部人工鼠标逐项验收；窗口行为依据程序内置回归与上述图像检查。

可复现日志：`dist/fix-regressions-2026-09-05.log`。2026-09-06 使用修正后的测试器再次完整运行 `verify_fix_regressions.ps1`，全部通过，日志为 `dist/final-fix-regressions-2026-09-06.log`。Linux 隔离检查与服务切换日志已下载到 `dist/linux-verification.log`、`dist/activation.log`。最终包 SHA-256 见 `dist/SHA256SUMS.txt`。

## 兼容与限制

- 版本为兼容性补丁 1.0.1；PC 协议按服务器能力协商启用新动作，不变更 ESP32 共享游戏协议。旧 PC 客户端仍可连接新服务器；新客户端连接旧服务器会回退旧动作，但无法得到 F01/F02 的完整服务端保障。
- 绑定文件与 SQLite 表格式保留；统计事件白名单新增使用区段事件，没有重写历史原始事件。拥堵时允许少量统计丢失，优先保证游戏；后台不应被解释为精确审计账本。
- 公网 TLS/Nginx 生效配置、ECS Linux 编译及重启后线上互通已验收。本机 WSL 枚举曾返回拒绝访问；后续使用用户授权的现有 SSH 私钥在 ECS 隔离目录编译，没有绕过 WSL 限制。
- 安装包未配置 Authenticode 签名，Windows 仍可能提示未知发布者。本轮未降低任何系统安全设置。
- 自动化检查通过只说明所覆盖用例未发现回归，不代表可保证所有系统、网络和断电条件下绝对无 bug。

## ECS 上线门槛（已完成；保留操作范围说明）

1. 确认维护窗口，备份当前桌宠二进制、网关脚本、Nginx 配置和绑定/统计存档；不触碰 `plane-link.service` 或 32100/UDP。
2. 在 ECS 编译 PC 匹配服务；执行 Python 语法/测试、`nginx -t` 和服务单元检查。保留现有证书、密码、环境变量和端口，仅合入本次相关限流/XFF 更改。
3. 短暂重启桌宠服务与网关并重载 Nginx；验证健康检查、原绑定恢复、同出口双端公网对战、取消竞争、表情和统计后台。
4. 如失败，恢复对应代码和配置，不清空正式绑定或统计。通过后再决定是否将签名更新清单发布到 OSS；本轮未上传新清单。

## 2026-09-05 晚间续检

- 用户确认同机另一项目为 FingerKnight。禁止修改 `/opt/fingerknight-server`、`/etc/fingerknight-server.env`、`fingerknight-server.service` 和 32200/UDP；原 `plane-link.service` 与 32100/UDP 也不在修改范围。不会重启整台 ECS 或覆盖共享 Nginx 总配置。
- 浏览器调宽后已通过目标实例 `i-7xv52nvaip4k5v4ohetk` 的云助手完成只读审计与第一份备份。后续用户提供现有 SSH 私钥路径，切换至已验证主机指纹的 SSH/SCP；没有修改密钥、凭据或安全组。
- 修正仅测试代码的 Linux 适配：`server_state_test.cpp` 避免重复定义 POSIX 常量；`server_edge_test.py` 支持 Linux 启动和显式 `--server` 路径。Windows 重新编译临时绑定状态测试通过，4 组隔离 UDP 功能回归通过，21 项网关/统计测试通过。
- 新增 `deploy/verify-linux-staging.sh`，只允许在 `/opt/plane-pet/staging/<release>` 内编译和测试。使用低优先级串行编译，凭据、统计与临时文件全部重定向到独立检查目录。ECS 上 g++ 11.4.0 无警告编译通过；服务端状态回归、21 项网关/统计测试及 4 组隔离 UDP 回归均通过。
- 已上传源码包 `dist/plane-pet-linux-source-1.0.1-20260905.tgz`（85032 字节），SHA-256：`fc1c9372bb136da45ffbe194d94b60cd9c7ebb8ecbd78e7eadb54f9e5813d044`，远端校验一致。包含桌面源码仅用于核对统计事件白名单，不含用户绑定、密码或统计数据。

## 2026-09-06 公网最终验收

- 2026-09-05 22:17:39（北京时间）完成桌宠 1.0.1 服务切换。仅替换桌宠二进制/网关脚本、网关单元中的小时登记配额，以及桌宠专属 Nginx 文件中 13 处已核实的限流/XFF 项。Nginx 语法检查通过，执行平滑重载，主进程没有重启；未覆盖共享总配置或证书。
- Linux 二进制 SHA-256：`59177e624f65532fb5d005c2dc3d6e17de7ae884e183c8c7bff016a2f262aa0e`；网关脚本 SHA-256：`a42c731a8ca2a8e3f9b1913b3dd03553b7d75ac782b783cc4a4785d0a088bad8`。
- 正式数据副本由新网关、再由旧网关读取与汇总，均通过；表结构、历史记录和凭据保持不变。上线时回环健康检查、公网证书验证、匿名后台访问 401、原账号授权访问 200 均通过。
- Windows 公网验收日志：`dist/public-online-check-6d1bad5cb1da/integration.log`。两个真实 WinHTTP TLS 隧道连接原 ECS，旧动作协议下匹配、在线、表情、邀请 300 秒、取消、重复序号拒绝、倒数、Playing、解绑均通过，协议/Socket/控制错误为 0。公网用例在 Playing 后解绑，没有等待实际 180 秒平局；平局与结算的完整流程在隔离回归中验证。
- 两个实际 `PlanePetClient.exe` 使用独立存档，通过新能力协商完成隐藏接邀与倒数开局；模拟单端进程退出后，重新启动自动恢复绑定并收到断线结算，双方战绩各一次，旧对局未恢复为 Playing。测试不上传匿名统计，未读取个人 AppData 绑定。
- 前两次公网验证发现的是测试器问题：版本兼容通知复用 Matched 状态但不含凭据，测试器误将自己的凭据清为零，导致测试解绑返回 BindingMissing；正式客户端不会这样覆盖。已修正测试器、增加错误码统计与幂等重试，重新编译后通过。交付客户端及压缩包无需因此重建。
- 清理了两次失败测试遗留的 2 组专用绑定和本轮 6 份测试凭据；未删除正式统计。最后逐条比对：原有 **3 组绑定、12 份联网凭据、1,128 条统计事件全部保持**，无新增测试绑定或凭据遗留。
- 第一份备份：`/opt/plane-pet/backups/fix-1.0.1-20260905-1335`；切换前最新备份：`/opt/plane-pet/backups/activation-1.0.1-20260905.L4bFjtmS`；测试清理前备份：`/opt/plane-pet/staging/fix-1.0.1-20260905-1335/test-cleanup-a20jgb5v`。备份仅限服务器管理员访问。
- 保护范围核查：FingerKnight 配置、env、current 指向与 ESP32 服务配置均未更改；`plane-link` PID 15737 保持。FingerKnight 在最初网页审计后、桌宠部署前已变为 inactive（PID 0），本任务没有停止或重新启动它，其状态在本次部署与复验中保持不变。
- systemd 校验曾提示已有 `snapd.service` 的 `RestartMode` 不被当前系统识别，与桌宠单元无关，本任务没有修改它。桌宠服务无自动崩溃重启、警告级日志无新增错误。
- 本地生产配置快照同步本轮同样的 13 处限流/XFF 修正，避免以后按旧快照部署时覆盖已上线修复。OSS 更新清单仍为 1.0.0，本轮没有发布更新推送，也没有 Git 提交/推送。
- 清理后的最终核查：桌宠服务 PID 57182、网关 PID 61538，均 active 且自动重启计数为 0；Nginx 主进程仍为 30712。再次执行公网 HTTPS 健康检查、`nginx -t`、部署文件及受保护文件 SHA-256 核查，均通过。所有本轮本机测试进程已退出。
