# 3.0.3 拖尾增强

日期：2026-09-08。只调整对战双翼拖尾及版本标记，不修改烟雾、命中、爆炸、移动、碰撞、网络、存档、统计或窗口尺寸。未修改服务器、发布自动更新或结束用户的旧版本进程。

## 改动

- 亮芯宽度 1 → 2px，增加 4px、20% 相对不透明度的像素外光；蓝红亮芯分别提亮。
- 生命周期 0.52 → 1.05 秒，后移速度 42 → 58px/s；静止时完整几何长度约 22 → 61px（实际可见尾端逐渐淡出，运动时随路径变化）。
- 淡出从平方改为 1.25 次幂，健康不透明度上限 0.82 → 0.96，1 血 0.30 → 0.38，仍保留明显的受损变淡区别。
- 每条翼尖轨迹容量 40 → 72 点，静态断言确保完整容纳 60Hz、1.05 秒历史；避免仅延长寿命却被旧容量提前截断。
- 两层光效共用同一真实移动路径，复用每层画笔；不新增或重绘位图素材。

## 本轮验证

- `verify_effects.ps1`：严格 `-Wall -Wextra -Werror` 编译、扩展模型测试、204 帧实际绘制、30 张资源帧透明度、双视角 HUD 像素一致、结算返回、命中去重和 GDI 稳定性通过。
- 新增 30/60/144 FPS 下保留完整长轨迹、尾段亮度、存活时长及 1 血淡出对比测试；烟雾计数及大小仍与 3.0.2 一致。
- 已读图检查正常、2 血和 1 血的弯曲拖尾，效果明显增强，未覆盖 HUD。
- `build.ps1 -BuildPublic`：全量构建通过。
- 单端/双端公网 ZIP 静态完整性、版本、图标、内嵌组件与新构建文件哈希检查通过。
- `verify_battle.ps1 -SkipLive`：移动核心 777476 项、战斗核心 1320309 项及 54 帧接触/最终状态实际绘制检查通过。图像记录在 `dist/battle-verification-109a770605a34b68863a44258a816695`。
- 本轮未复测公网对局或全套 702 项窗口验收，不沿用上轮结果冒充本轮通过。

## 交付

- `dist/PlanePet-Public-DualLocal-3.0.3.zip`，SHA256 `4948a9b857e9ff8e92240cb7ccb5f09b335e0c25a05ccdba519e3fbee89fd325`。
- `dist/PlanePet-Public-Single-3.0.3.zip`，SHA256 `03502f1ba148039753504c44e14a710e8444a4f07aa7eb61160fc3bb77b72768`。
- `dist/vfx-verification-3.0.3/actual-game-effects.gif`：生产绘制函数生成的双视角示范，非公网录像。

使用前从菜单退出旧版，再解压运行新包内的 PlanePet.exe。本次没有重新签名，仍为未签名测试版本。历史 3.0.2 交付包及其验收文件保留不动。
